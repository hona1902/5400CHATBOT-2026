# Current Phase — GraphRAG programme status

| Phase | Status | Deliverable |
|---|---|---|
| Phase 0 — Governance & tooling baseline | ✅ **COMPLETE** | `AGRIBANK.md`, `DECISIONS.md`, Graphify baseline |
| GraphRAG-01 — Forensic & architecture decision | ✅ **COMPLETE** | [`../architecture/GRAPHRAG_FORENSIC.md`](../architecture/GRAPHRAG_FORENSIC.md) rev-2 · [`GRAPHRAG_DECISION.md`](GRAPHRAG_DECISION.md) (AGR-005) |
| GraphRAG-02 — Isolated LightRAG PoC | ✅ **COMPLETE** — accepted 2026-08-27 | [`../architecture/GRAPHRAG_POC.md`](../architecture/GRAPHRAG_POC.md) |
| GraphRAG-03 — Indexing lifecycle & durable deletion | ✅ **COMPLETE** — forensic approved 2026-08-28; slices 03-A **COMPLETE** · 03-B **COMPLETE** · 03-C **COMPLETE** · 03-D **COMPLETE** · 03-E **COMPLETE / APPROVED** | [`../architecture/GRAPHRAG_LIFECYCLE_FORENSIC.md`](../architecture/GRAPHRAG_LIFECYCLE_FORENSIC.md) · [`GRAPHRAG_03A_INDEXING.md`](GRAPHRAG_03A_INDEXING.md) · [`GRAPHRAG_03B_DURABLE_DELETE.md`](GRAPHRAG_03B_DURABLE_DELETE.md) · [`GRAPHRAG_03C_TOMBSTONE_DRAIN.md`](GRAPHRAG_03C_TOMBSTONE_DRAIN.md) · [`GRAPHRAG_03D_RECONCILE.md`](GRAPHRAG_03D_RECONCILE.md) · [`GRAPHRAG_03E_REBUILD.md`](GRAPHRAG_03E_REBUILD.md) |
| GraphRAG-04 — Synthetic retrieval evaluation / quality baseline | ✅ **COMPLETE / APPROVED** — signed off 2026-08-30. Frozen synthetic benchmark v1 (14 sources / 28 queries / 6 classes / DEV 17 · HOLDOUT 11) + eval-only harness; **live synthetic baseline EXECUTED** (OpenRouter `text-embedding-3-small` + `gpt-4o-mini` through LightRAG v1.5.6; both probe gates PASS; cleanup+restore verified). **RRF_CANDIDATE_INTERFACE_READY = NO** (LightRAG refs unordered/no score, live-confirmed `ordered=False`); **HYBRID_VALUE_EVIDENCED = INCONCLUSIVE** (graph returns a broad unordered candidate surface ≈11/14 sources per query — incl. distractor/negative — so 1.0 source-hit/recall is coverage, not precision). Karpathy CLEAN · Codex A/B/C resolved (no unresolved actionable HIGH). GraphRAG-04 mypy surface **clean**; full-project `mypy .` still reports **91 pre-existing errors in untouched GraphRAG-03 test files** (04 adds zero). **NO migration** (count 50). | [`GRAPHRAG_04_EVALUATION.md`](GRAPHRAG_04_EVALUATION.md) |
| GraphRAG-05 — Ranked graph candidate surface (FORENSIC / DESIGN GATE) | ✅ **FORENSIC/DESIGN COMPLETE / APPROVED** — forensic approved 2026-08-30 (tag `graphrag-05-forensic-approved`, commit `833ec59`); not an implementation phase. Traced the full pinned LightRAG **v1.5.6** (commit `b33c6b0`) query pipeline `/query → aquery → kg_query → _build_query_context → convert_to_user_format`. Finding: every genuine query-relevance score (entity/relation/chunk embedding **cosine**, cross-encoder **rerank_score**) is **computed then dropped before any HTTP surface**; the only numerics that reach the API are **structural** (relationship `weight`, node degree) and **frequency** (`reference_id`). Hybrid final order is a **round-robin interleave** (not a rank). New vs 04: the **`/query/data`** structured endpoint (entities/relationships/chunks/references, no LLM answer) gives **richer provenance but still no relevance score**. Decisions: `LIGHTRAG_DIRECT_SCORED_SOURCE_SURFACE = NO`; `LOWER_LEVEL_QUERY_SCORE_AVAILABLE = PARTIAL` (internal-only, unexposed); `SOURCE_PROVENANCE_FOR_SCORED_EVIDENCE = STRONG` (chunk) / PARTIAL (kg); `SOURCE_LEVEL_AGGREGATION_DEFENSIBLE = REQUIRES_EXPERIMENT`; `GRAPH_NATIVE_RANKING_SIGNAL = NO`; `ABSTENTION_SIGNAL_AVAILABLE = UNCLEAR`; `GRAPH_CANDIDATE_CONTRACT_DESIGNABLE = YES` (unranked) / NO (ranked); `GRAPH_CANDIDATE_IMPLEMENTATION_READY = NO`; **`RRF_CANDIDATE_INTERFACE_READY = NO`**. Preferred **Option C** — expose GraphRAG as an **unranked, provenance-strong evidence set** (`GraphSourceCandidate`, no score field); Option D (surface a real score) requires modifying pinned LightRAG and is out of scope. **No production/test/migration/API/frontend code; zero provider traffic; no DB / LightRAG-storage mutation; sidecar not started; `GRAPHRAG_ENABLED` stayed false; no migration (count 50).** | [`GRAPHRAG_05_RANKED_CANDIDATE_FORENSIC.md`](GRAPHRAG_05_RANKED_CANDIDATE_FORENSIC.md) |
| GraphRAG-06 — Structured evidence surface (FORENSIC / DESIGN GATE) | ✅ **FORENSIC/DESIGN COMPLETE / APPROVED** — signed off 2026-08-30; **not an implementation phase; implementation NOT started** (`STRUCTURED_EVIDENCE_IMPLEMENTATION_READY=NO`). Question: should ON adopt a **structured evidence seam** on pinned LightRAG **v1.5.6** (`b33c6b0`) `/query/data` instead of the answer-generating `client.query()` (`/query`)? Traced both against pinned source. **§7 answer-generation separation = classification B** (`NO_FINAL_ANSWER_LLM_BUT_OTHER_LLM_CALLS_REMAIN`): `/query/data` forces `only_need_context=True` (`lightrag.py:3817`) and early-returns at `operate.py:4275` **before** the final-answer LLM, but the **keyword-extraction LLM** (`operate.py:4234→4618`) + query embeddings still run. **Retrieval-semantics parity = YES** — `/query` (`aquery_llm`) and `/query/data` (`aquery_data`) call the **identical `kg_query`** (only diff = `only_need_context`). **Failure isolation:** `/query` maps any generation exception to HTTP 500 (`query_routes.py:580-582`), discarding evidence already computed — `/query/data` removes that surface. **Provenance** STRONG(chunk·reference)/PARTIAL(entity·relation). **Data minimization = PARTIAL** (lower provider egress; but `chunks[].content`=RAW_SOURCE_TEXT in sidecar→ON response → needs ON-side projection). Decisions: `QUERY_DATA_AVAILABLE=YES`; `AVOIDS_FINAL_ANSWER=YES`; `OTHER_LLM_REMAIN=YES`; `PARITY=YES`; `RANK=NO`/`SCORE=NO` (frozen); `CORPUS_MUTATION=NO`; `CACHE_MUTATION=CONFIG_DEPENDENT`; `CONTRACT_DESIGNABLE=YES`; `IMPL_READY=NO`; **`PREFERRED=B`** (design target; C fallback if a generated-answer consumer appears — none today; ON already discards `/query`'s answer); `GRAPH_RAG_ROLE=UNRANKED_EVIDENCE_ENGINE+PROVENANCE_ENRICHER+CONTEXT_EXPANDER`; `RRF_CANDIDATE_INTERFACE_READY=NO`. **No production/test/migration/API/frontend code; zero provider traffic; no DB / LightRAG-storage mutation; sidecar not started; `GRAPHRAG_ENABLED` stayed false; no migration (count 50).** Pinned source re-cloned read-only (public tag v1.5.6, commit verified `b33c6b0`) into out-of-repo scratchpad. | [`GRAPHRAG_06_STRUCTURED_EVIDENCE_FORENSIC.md`](GRAPHRAG_06_STRUCTURED_EVIDENCE_FORENSIC.md) |
| GraphRAG-07 — Structured evidence adapter contract & implementation-readiness gate (FORENSIC / ARCHITECTURE / CONTRACT DESIGN) | 🟨 **CONTRACT-FREEZE GATE + INDEPENDENT REVIEW COMPLETE — awaiting final sign-off; not an implementation phase; implementation NOT started.** Ran in three passes (same phase): a **design pass** (anti-corruption adapter contract + data-minimization), a **contract-freeze / implementation-readiness gate**, and an **independent contract review** (fresh self-review + independent Codex subagent). **Independent review (2026-08-30): 14 documentation-only findings (7 HIGH cross-doc contradictions, 5 MEDIUM, 2 LOW) — none a safety/security defect — all dispositioned with doc-only fixes** (authoritative reconciliation §2a: type/field names, 5-state `ProvenanceQuality`, CANCELLED = propagate `asyncio.CancelledError` unwrapped, `supporting_chunk_count` REQUIRED-≥1-when-present; HARD `STALE_SOURCE_CONSUMER_LOOKUP_REQUIRED=YES` at citation layer; `evidence_types`-cardinality no-rank clause; companion doc marked SUBORDINATE/HISTORICAL). Outcome: `CONTRACT_AND_SAFETY_READY=YES`, `VALUE_EVIDENCE_READY=NO`, `STRUCTURED_EVIDENCE_IMPLEMENTATION_READY=NO` (unchanged). Next unresolved gate = **larger-corpus structured-evidence value evaluation** (on existing wired query surfaces; must precede any adapter build; GraphRAG-08 not started). **Verdict: `STRUCTURED_EVIDENCE_CONTRACT_FROZEN=YES` but `STRUCTURED_EVIDENCE_IMPLEMENTATION_READY=NO`** — every safety/operational policy is frozen and no §76 stop condition fired; readiness is withheld only because value is UNEVIDENCED (`HYBRID_VALUE_EVIDENCED=INCONCLUSIVE` 04, `SOURCE_LEVEL_AGGREGATION_DEFENSIBLE=REQUIRES_EXPERIMENT` 05) and this freeze still awaits independent review (neither a safety block). Frozen contract: `GraphEvidenceResult{sources:frozenset[GraphSourceEvidence{source_id, evidence_types{DIRECT_CHUNK,GRAPH_ENTITY,GRAPH_RELATIONSHIP}, supporting_chunk_count(FREQUENCY, not relevance), provenance_quality}], diagnostics, status(SUCCESS\|EMPTY\|DEGRADED; FAILURE=exception)}` — **`RANK`/`SCORE` NO, rank impossible by construction (unordered set)**. Frozen policies: `RAW_LIGHTRAG_SCHEMA_EXPOSED_UPSTREAM=NO` / `NORMALIZED_EVIDENCE_ADAPTER_REQUIRED=YES` (`VENDOR_SCHEMA_BOUNDARY=client.py`); text ID/provenance-only (`RAW_CHUNK_TEXT=NEVER`, descriptions dropped); `EVIDENCE_PERSISTENCE_POLICY=TRANSIENT_ONLY` (no table/migration); provenance STRONG\|PARTIAL\|INVALID\|FOREIGN\|UNKNOWN, STRONG-only emission, foreign/malformed/unknown=DROP_AND_REPORT (schema breach=FAIL_CLOSED); dedup by `source_id`; `ORDER_SEMANTICS=NONE`; error model reuses existing `GraphRAGError` types; content-free diagnostics (Source-ID DEBUG_ONLY); config reuse `GraphRAGConfig`; `FEATURE_FLAG=F1+F3` (reuse `GRAPHRAG_ENABLED`, additive method, no new lock); timeout reuse (30 s), cancellation propagated, `RETRY=CALLER_OWNED`; `WORKSPACE=default single`; stale/deleted Source = emit id + consumer resolves, **no DB lookup, no query-path mutation**; `CORPUS_MUTATION=NO`; rollback additive/no-migration; `client.query()` untouched (backward-compatible); `LIGHTRAG_SCHEMA_VERSION_SHIELD=HYBRID`; `ADAPTER_PAYLOAD_LIMITS=REQUIRES_IMPLEMENTATION_EVALUATION`; `PREFERRED_ADAPTER_OPTION=C` (Source-only projection). Reviews A–H (design) + A–G (freeze: minimality, vendor leakage, provenance safety, false rank, privacy, rollback, phase boundary) all PASS; `RRF_CANDIDATE_INTERFACE_READY=NO`; `GRAPH_CANDIDATE_IMPLEMENTATION_READY=NO`. **No production/test/migration/API/frontend/GraphRAG-client code; zero provider traffic; no DB / Source / LightRAG-storage mutation; sidecar not started; `GRAPHRAG_ENABLED` stayed false; no migration (count 50); pinned LightRAG not re-cloned or moved into repo.** | [`GRAPHRAG_07_STRUCTURED_EVIDENCE_CONTRACT.md`](GRAPHRAG_07_STRUCTURED_EVIDENCE_CONTRACT.md) (authoritative) · [`GRAPHRAG_07_STRUCTURED_EVIDENCE_ADAPTER_CONTRACT.md`](GRAPHRAG_07_STRUCTURED_EVIDENCE_ADAPTER_CONTRACT.md) (design companion) |
| GraphRAG-08 — Larger-corpus structured-evidence value evaluation (BENCHMARK-DESIGN GATE) | 🟨 **DESIGN GATE COMPLETE — awaiting operator approval to execute; not an execution phase; benchmark NOT run; adapter NOT implemented.** Designs (does not run) a larger-corpus benchmark to answer whether structured GraphRAG evidence has enough real value to justify building the GraphRAG-07 adapter. Target `STRUCTURED_EVIDENCE_VALUE_EVIDENCED = YES/NO/INCONCLUSIVE` (unresolved here — this gate produces only the *means* to answer it). **Frozen design:** corpus **75** canonical Sources (drives graph candidate_fraction from 04's ~0.79 → ~0.15 so returning most of the corpus can no longer buy perfect recall; GraphRAG-05 §12 recommended ≥60–100); **60** queries across **10** classes (adds ENTITY/RELATIONSHIP/DISTRACTOR-term/BROAD-entity collisions + PARTIAL_EVIDENCE to 04's 6); **12** negatives over 6 constructions; **DEV 30 / HOLDOUT 30** (50/50, every class in both splits; HOLDOUT authoritative, no post-hoc tuning). **Systems V + GQ + GD:** V=`VECTOR_BASELINE` (ranked, real `vector_search`), GQ=current `client.query()` references (already wired via `GraphRAGEvalRunner`, **zero new code**), GD=`/query/data` structured evidence (**NOT wired in ON — only the sidecar has `POST /query/data`**; needs a future *eval-only* seam under `integrations/graphrag/eval/`, designed here, **not implemented**, never a production adapter). **Graph evidence is an UNORDERED SET — no MRR/nDCG/Hit@K-by-order on GQ/GD** (`RANK=NO`/`SCORE=NO` frozen); vector keeps ranked metrics. **Anti-broad-candidate core:** `candidate_fraction` (denominator = 75), `set_precision`, `false_positive_count`, negative `candidate_count`, breadth **distributions** (median/p75/p90/p95/max), + the precision-preserving rule — a system returning most of the corpus scores low precision / high fraction and **cannot** reach VALUE=YES. Multi-source GT (REQUIRED vs OPTIONAL, conservative), `FULL_SOURCE_SET_RECOVERED`, complementarity (FULL vs PARTIAL kept separate), oracle-union as upper bound only (no RRF conclusion). Provenance 5-state (STRONG-only admitted) + `OFF_BENCHMARK` allowlist accounting. Runtime value axis (`STRUCTURED_QUERY_DATA_RUNTIME_VALUE`): GQ triggers the discarded final-answer LLM, GD (`only_need_context=True`) does not → measured provider-call/latency/failure-isolation delta. **Isolation:** Option **A** (dedicated temporary Surreal namespace/DB, makes candidate_fraction exact, foreign Sources absent) with 04's per-id tag/allowlist/fail-closed-cleanup retained as a second layer; real canonical Source-creation → processing → vector index → GraphRAG lifecycle path (no indexing shortcut). Full run state machine (PLANNED→PREFLIGHT→…→CLEANUP→COMPLETE) with two-stage micro-precheck→full-corpus, no-retry policy, budget guard, content-free `.artifacts/` manifest + recovery, temp-model capture/restore. Value decision framework/matrix + Reviews A–G (leakage / broad-candidate gaming / graph-native validity / negative quality / isolation / cost / value-logic) all **Design PASS** (C & D carry empirical re-verification at fixture-authoring). **`GRAPH_RAG_08_EXECUTION_READY=YES` (design complete) — but operator approval still required before any live run.** Retained unchanged: `CONTRACT_AND_SAFETY_READY=YES`, `VALUE_EVIDENCE_READY=NO`, `STRUCTURED_EVIDENCE_IMPLEMENTATION_READY=NO`, `QUERY_DATA_EXPOSES_VALID_RANK/SCORE=NO`, `RRF_CANDIDATE_INTERFACE_READY=NO`, `GRAPH_CANDIDATE_IMPLEMENTATION_READY=NO`. **No production/test/fixture/eval/migration/API/frontend code; zero provider traffic; no DB / Source / LightRAG-storage mutation; sidecar not started; `GRAPHRAG_ENABLED` stayed false; no `.env` change; no migration (count 50).** | [`GRAPHRAG_08_LARGER_CORPUS_VALUE_EVALUATION_DESIGN.md`](GRAPHRAG_08_LARGER_CORPUS_VALUE_EVALUATION_DESIGN.md) |
| GraphRAG-08 — Fixtures + evaluation-only harness (OFFLINE checkpoint) | 🟨 **OFFLINE FIXTURE + HARNESS FROZEN / APPROVED — micro-precheck DEFERRED (not run); not a value run; adapter NOT implemented.** Authored the frozen **`graphrag_08_eval_v1`** benchmark: **75** synthetic canonical Sources (6 invented clusters — Helix Robotics / Marisol Seaport / Verdant Agritech / Aurora University / Cobalt Financial / distractor block; relevant hub Talos-9, high-degree distractor hub Standards Bulletin 7; Dr. Lena Marsh / Meridian / MRC / "certifies" collisions; cross-cluster bridge chains) and **60** queries across **10** classes (**30 DEV / 30 HOLDOUT**, every class in both splits; **12** negatives spanning all 6 constructions; GT cardinality 36×1 / 6×2 / 6×3). Ground truth is **manual, Source-level, minimal-required vs optional-support**, authored before any retriever. **R1** (graph-native: every two_hop req≥2 / three_hop req≥3, no single Source answers) and **R2** (negatives unambiguously unsupported) verified from the corpus alone. Integrity-frozen: **`FIXTURE_INTEGRITY = a58a68535c345e18f0263904f818e4e2068a164056408665d8bb9233eceb143d`** (sha256; any content change ⇒ `graphrag_08_eval_v2`). **Eval-only harness** under `open_notebook/integrations/graphrag/eval/` (new, never imported by production): `dataset08` (loader/validator/freeze), `metrics08` (set precision/recall/f1, candidate breadth distributions, full/partial recovery, FULL-recovery complementarity — **permutation-invariant, no rank/score**), `gd_seam` (**eval-only** LightRAG `/query/data` caller via its own httpx from `GraphRAGConfig`, STRONG-anchor projection, raw vendor schema contained — never logged/persisted/returned, `only_need_context=True`, `ORDER_SEMANTICS=NONE`), `runner08` (V/GQ/GD, DEV-only precheck subset selection under ≤8-source/≤6-query caps, content-free run manifest, fail-closed cleanup), `report08` (content-free artifact, `value_run=NO`/`holdout_used=NO`). **Independent adversarial review: no HIGH**; MEDIUM-1/2/3 + LOW-4/5 (GT quality) all fixed **pre-freeze**. **Tests:** 33 GraphRAG-08 offline tests PASS; GraphRAG regression (flag off) **444 pass / 8 skip / 0 fail**; ruff + targeted mypy clean. **`client.query()`, `vector_search`, and every frozen v04 eval module UNCHANGED; production imports no eval code; no new prod `query_data`/`query_evidence`/`GraphEvidenceResult`.** **`MICRO_PRECHECK_STATUS = DEFERRED_NOT_RUN` · `GRAPH_RAG_08_MICRO_PRECHECK_PASS = NO` · `GRAPH_RAG_08_FULL_EXECUTION_READY = NO`.** **BLOCKER:** the approved GraphRAG-08 isolation policy requires **Option A — a dedicated temporary Surreal namespace/database**; `runner08` currently follows the earlier **Option-B normal-DB** tag+allowlist+fail-closed pattern, which is **NOT approved for GraphRAG-08 live execution**. Operator **deferred** the live precheck rather than write benchmark Sources to the normal DB. **Next phase: GraphRAG-08A — Dedicated Temporary Surreal Isolation for the Evaluation Harness** (not started). Retained: `CONTRACT_AND_SAFETY_READY=YES`, `VALUE_EVIDENCE_READY=NO`, all `*_VALUE_EVIDENCED=NOT_RUN`, `STRUCTURED_EVIDENCE_IMPLEMENTATION_READY=NO`, `QUERY_DATA_EXPOSES_VALID_RANK/SCORE=NO`, `RRF_CANDIDATE_INTERFACE_READY=NO`, `GRAPH_CANDIDATE_IMPLEMENTATION_READY=NO`. **No provider traffic; sidecar not started; no DB / Source / LightRAG-storage mutation; `GRAPHRAG_ENABLED` stayed false; no `.env` change; no migration (count 50).** | [`GRAPHRAG_08_LARGER_CORPUS_VALUE_EVALUATION_DESIGN.md`](GRAPHRAG_08_LARGER_CORPUS_VALUE_EVALUATION_DESIGN.md) · `tests/fixtures/graphrag_08_eval_v1/` |
| GraphRAG-08A — Dedicated temporary Surreal isolation (eval harness) | 🟨 **ISOLATION IMPLEMENTED + INDEPENDENTLY REVIEWED — awaiting checkpoint; micro-precheck still NOT run; not a value run.** Solved the only remaining precheck blocker: the live micro-precheck must not write to the normal application Surreal namespace. **Forensic:** `db_connection` opens a fresh `AsyncSurreal` per call and reads `SURREAL_NAMESPACE`/`SURREAL_DATABASE` from env at connect time — **no singleton, no pooling** — so a **process-local env override** cleanly redirects every `repo_*` + the migration bootstrap to a temp namespace with nothing to rebind; schema is bootstrapped via the canonical `AsyncMigrationManager` (**no migration added; count stays 50**); and the runner indexes **in-process** (`embed_source_command` awaited directly; `service.index_source` = direct HTTP), so the surreal-commands **worker is not on the precheck path**. **Implemented** (eval-only `open_notebook/integrations/graphrag/eval/isolation08.py`): `isolated_surreal_eval_runtime` context manager — temp names `graphrag_eval_<run_id>`/`graphrag_08_<run_id>`, **normal-DB hard guard** (`assert_not_normal`, rejects identity + shared-namespace overlap), env override, canonical schema bootstrap (verify version==25), **guarded owned cleanup** (`REMOVE NAMESPACE IF EXISTS` only for the run-owned temp identity, never normal, idempotent), env restore **verified**, reentrancy refused, 5 typed errors, content-free logging; plus `require_active_isolation()` wired into `runner08.create_and_index` as an **Option-B hard block**. **Proven against the real local SurrealDB (temp namespace only):** bootstrap→v25, isolated write, **normal DB unchanged**, temp dropped, env restored. **Storage boundaries:** canonical + vector (`source_embedding`/`fn::vector_search`) **ISOLATED** by the namespace override; LightRAG storage honestly **SHARED_BUT_OWNED** (per-run unique ids + per-id cleanup; dedicated workspace = optional future hardening, not done — no LightRAG change); worker/queue **NOT used** by the precheck. **Independent review: no normal-DB mutation path; one HIGH functional (fail-closed) guard defect + 2 LOW — all FIXED** (guard now compares against captured normal/temp identities, `finally` restores before cleanup await, enter guard rejects shared-namespace overlap) with added regression tests. **Tests:** 13 GraphRAG-08A pass (7 unit + 6 live-gated against real SurrealDB); GraphRAG regression (flag off) **455 pass / 8 skip / 0 fail**; ruff + mypy clean; **fixture hash unchanged** (`a58a6853…143d`); no production import of eval; migrations unchanged. **`SURREAL_OPTION_A_ISOLATION_IMPLEMENTED=YES` · `GRAPH_RAG_08A_READY_FOR_MICRO_PRECHECK=YES`** — with a documented precheck prerequisite (the precheck must seed a temporary embedding Model into the isolated namespace via the normal path, and handle LightRAG per-run ownership+cleanup — by design out of 08A scope, §32/§48/§33). **`MICRO_PRECHECK_STATUS=DEFERRED_NOT_RUN` · `GRAPH_RAG_08_FULL_EXECUTION_READY=NO`** (unchanged — 08A readiness authorizes only a future separate operator decision to run ≤8 Sources / ≤6 DEV / HOLDOUT=0, never 75×60). Retained: `CONTRACT_AND_SAFETY_READY=YES`, `VALUE_EVIDENCE_READY=NO`, all `*_VALUE_EVIDENCED=NOT_RUN`, `STRUCTURED_EVIDENCE_IMPLEMENTATION_READY=NO`, `QUERY_DATA_EXPOSES_VALID_RANK/SCORE=NO`, `RRF_CANDIDATE_INTERFACE_READY=NO`, `GRAPH_CANDIDATE_IMPLEMENTATION_READY=NO`. **No provider traffic; sidecar not started; `GRAPHRAG_ENABLED` false; no `.env` edit (process-local override, restored+verified); no migration; no production code change; no fixture edit; local temp-Surreal test mutation only.** | [`GRAPHRAG_08A_TEMP_SURREAL_ISOLATION.md`](GRAPHRAG_08A_TEMP_SURREAL_ISOLATION.md) |
| GraphRAG-08 — Authorized live micro-precheck | ✅ **MICRO-PRECHECK PASS — bounded live execution-correctness run COMPLETE; NOT a value run; full 75×60 benchmark NOT run.** One operator-authorized provider-backed micro-precheck under GraphRAG-08A Option-A isolation (run_id `c531cf98a092`). **Hard caps respected:** 8 synthetic Sources (S002/S007/S021/S030/S039/S040/S052/S055), 6 DEV queries (direct/two-hop/three-hop-cross-source/entity-collision/relationship-collision/negative), **HOLDOUT=0**. Ran the real path: temp Surreal namespace (schema bootstrap v25) → temp OpenRouter embedding Model seeded in the isolated DB (openai/text-embedding-3-small, **observed dim 1536**) → pinned LightRAG **v1.5.6** sidecar (started, healthy, stopped after) → canonical Source creation + vector embedding + GraphRAG indexing (in-process) → **V / GQ / GD** for all 6 DEV queries. **Results (execution-correctness only, NOT value):** V hit@1=0.4/hit@5=1.0/full_set@5=1.0; GQ=GD set_recall=1.0, set_precision=0.204, candidate_fraction median=1.0 (**expected 8-source small-corpus artifact**, correctly penalised by precision — explicitly not a value signal); **GD final-answer invariant held** (only_need_context); **GQ↔GD parity: identical Source sets on all 6** (gq_only=0/gd_only=0 — empirically confirms GraphRAG-06 parity, no blocker); negative Q43 non-empty (8 cands) correctly NOT success; provenance all valid (0 foreign/malformed). **Content-free artifact** under gitignored `.artifacts/` (forbidden-token scan clean). **Cleanup/restore PASS:** LightRAG per-id owned deletes, temp namespace dropped (removes Sources+Model), sidecar stopped, GRAPHRAG_ENABLED restored false; **normal DB unchanged (1→1)**, no temp model in normal DB, no `graphrag_eval_*` residue; **fixture hash before==after (`a58a6853…143d`)**. Post-cleanup offline regression: graphrag suite **457 pass / 8 skip / 0 fail**; ruff + mypy clean. Note: a first launch aborted on an eval-only launcher `sys.path` bug before any benchmark work (1 dim-probe call only), cleaned up fully; the launcher was fixed and the benchmark ran exactly once. **`GRAPH_RAG_08_MICRO_PRECHECK_PASS=YES` ⇒ `GRAPH_RAG_08_FULL_EXECUTION_READY=YES`** (technically ready; a separate operator authorization is still required to run the full 75×60). Retained: `VALUE_EVIDENCE_READY=NO`, all `*_VALUE_EVIDENCED=NOT_RUN`, `STRUCTURED_EVIDENCE_IMPLEMENTATION_READY=NO`, `QUERY_DATA_EXPOSES_VALID_RANK/SCORE=NO`, `RRF_CANDIDATE_INTERFACE_READY=NO`, `GRAPH_CANDIDATE_IMPLEMENTATION_READY=NO`. **Provider traffic = micro-precheck only; no HOLDOUT; no full benchmark; no production adapter; no migration; no `.env` edit (process-local overrides restored); no fixture edit.** | eval-only `open_notebook/integrations/graphrag/eval/precheck08.py` |
| GraphRAG-08 — Full 75×60 value benchmark (5 attempts) + retry/observability/preflight hardening | 🟥 **ALL FIVE FULL-RUN ATTEMPTS FAILED AT OR BEFORE QUERY — no value decision; harness hardened offline (08B/08C committed+tagged, 08D offline); a fresh explicit operator authorization is required (effectively attempt #6).** Five distinct attempts, none a value result, not aggregated: **#1** (`8c9d83c77d92`, FAILED_BEFORE_QUERY) — Source S002 hit LightRAG `DocStatus.FAILED` (pre-hardening runner aborted on first failure). **#2** (`03bc96689656`, FAILED_BEFORE_QUERY) — a *different* Source S001 hit `DocStatus.FAILED`, classified **NON_RETRYABLE** by the frozen classifier → fail-closed, no partial corpus. **#3** (launcher defect, FAILED_AT_PREFLIGHT) — a standalone full-run process did **not** load `.env`, so the normal-DB baseline read collapsed to `null` (SurrealDB signin failed and was swallowed); **hardened by GraphRAG-08C** (fail-closed baseline preflight). **#4** (`2f783c62d6dc`, FAILED_BEFORE_QUERY) — passed baseline gate + sidecar + Option-A isolation + schema v25 and **created + vector-embedded staging for all 75 Sources**, then died in `_vector_embed_all` at `from commands.embedding_commands import …` → `ModuleNotFoundError: No module named 'commands'` (repo root absent from the standalone driver's import path); **hardened by GraphRAG-08D**; **graph indexing NOT reached** (`per_source_attempts={}`). **#5** (`13e59a3edbb8`, auth label REAUTHORIZATION_5, FAILED_BEFORE_QUERY) — the **first post-launcher-fix** full run: corrected launcher worked end-to-end, normal-DB baseline concrete + unchanged, sidecar healthy, Option-A isolation PASS, schema v25, embedding dim 1536, **all 75 Sources created + vector-embedded**, and **GraphRAG indexing WAS reached (all 75 attempted)** — then logical Source **S001** (`source:gr08ef5d8978700`) failed at the **TRACK** surface with LightRAG `DocStatus.FAILED`, classified **NON_RETRYABLE** (reason `TRACK_TEXT_PRESENT_NO_ALLOWLIST_MATCH`; error-text present, length bucket 129–256, no transient/non-transient allowlist match, `retry_allowed=false`, S001 attempt_count=1, max_attempts_observed=1); the hard **75/75 complete-corpus gate** then aborted before any query (partial corpus never used, `graphrag_indexed_count=0`). **#4 vs #5 (critical distinction):** #4 was a launcher/import defect where **graph indexing was never reached**; #5 was a **genuine GraphRAG extraction failure on S001** where **graph indexing WAS reached** — empirically confirming the 08D launcher fix carried a real run past the #4 failure point. Every attempt: **0 DEV / 0 HOLDOUT executed, VALUE_DECISION_MADE=NO**, cleanup + restoration PASS, normal DB unchanged, fixture hash unchanged. #1/#2/#5 were provider/indexing (extraction) failures (S002 / S001 / S001) — #5 at the TRACK surface with indexing reached; #3/#4 were **eval-only launcher defects** with indexing never reached, not benchmark/provider failures. **Eval-only harness hardening (offline, independently reviewed, COMMITTED + TAGGED through 08C):** (1) **index-retry** — `MAX_INDEX_ATTEMPTS_PER_SOURCE=2` bounded transient retry (single per-Source counter across submit+track; reindex = delete-then-insert; fail-closed on unknown/ambiguous) + hard **75/75 complete-corpus gate**; (2) **GraphRAG-08B observability** (`1d74fa4`, tag `graphrag-08b-observability-approved`) — content-safe `FailureDiagnostic` (surface/attempt/classification/reason-code/coarse class enums/length-bucket/http-status-class/exception-type — **never raw error text**), failure telemetry that **survives a pre-ANALYZE abort** (atomic write before cleanup); (3) **GraphRAG-08C preflight** (`d1d9d65`, tag `graphrag-08c-preflight-approved`) — fail-closed normal-DB baseline (identity + concrete count + model presence; `null==null` never "unchanged") + content-safe SIDECAR_START diagnostics; (4) **GraphRAG-08D launcher/import-path** (offline, this row) — deterministic `Path(__file__)` repo-root resolver + fail-fast import-path preflight verifying the exact `commands.embedding_commands` surface **before** normal-DB/sidecar/isolation/Source-creation/provider, closing the #4 defect. **Retry DECISION, allowlist, max-attempts, concurrency, provider/model, fixture all UNCHANGED** (S001's #5 non-retryable classification is the frozen classifier working as designed, not a policy change). **Tests:** GraphRAG regression (flag off) **522 pass / 8 skip / 0 fail**; ruff + targeted mypy clean; fixture hash `a58a6853…143d` unchanged. **`GRAPH_RAG_08D_READY_FOR_FULL_RUN_REAUTHORIZATION=YES` (harness diagnostically ready) but `FULL_EXECUTION_AUTHORIZED=NO`.** **Authorized S001 forensic:** one isolated forensic S001 re-index (raw LightRAG track error read EPHEMERALLY, never persisted/logged/hashed) was **AUTHORIZED**; no evidence of execution found (no artifact — an ephemeral run persists none by design — no doc/commit) ⇒ `S001_FORENSIC_EXECUTED=NO` (not run in this reconciliation). Retained: `VALUE_EVIDENCE_READY=NO`, all `*_VALUE_EVIDENCED=NOT_RUN`, `STRUCTURED_EVIDENCE_IMPLEMENTATION_READY=NO`, `QUERY_DATA_EXPOSES_VALID_RANK/SCORE=NO`, `RRF_CANDIDATE_INTERFACE_READY=NO`, `GRAPH_CANDIDATE_IMPLEMENTATION_READY=NO`. Historical runs preserved separately (not aggregated, not value results). **No fixture edit; no allowlist/concurrency/policy change; no provider traffic in the hardening phases; sidecar stopped; `GRAPHRAG_ENABLED` false; no migration; no production code change.** | [`GRAPHRAG_08B_FAILURE_CLASSIFICATION_OBSERVABILITY.md`](GRAPHRAG_08B_FAILURE_CLASSIFICATION_OBSERVABILITY.md) · [`GRAPHRAG_08D_LAUNCHER_IMPORT_PREFLIGHT.md`](GRAPHRAG_08D_LAUNCHER_IMPORT_PREFLIGHT.md) |
| GraphRAG-08D — Full-run launcher & import-path fail-fast preflight (OFFLINE hardening) | 🟨 **OFFLINE HARDENING COMPLETE — awaiting independent-review sign-off + checkpoint; not a value run; full-run gate CLOSED.** Root-caused attempt #4: `commands` is a top-level repo package excluded from the editable install (`pyproject` `package-dir` maps only `open_notebook`), imported lazily inside `runner08._vector_embed_all`; a driver whose `sys.path[0]` is not the repo root cannot import it, so the failure surfaced only after 75 Sources were created. New eval-only `launcher_preflight08.py`: `resolve_repo_root()` (deterministic from `Path(__file__)`, cwd-independent), process-local `sys.path` self-heal (never `PYTHONPATH`/env), and a fail-closed `run_launcher_preflight()` verifying the exact `commands.embedding_commands:{EmbedSourceInput,embed_source_command}` surface. Wired into `run_full_benchmark` **after fixture verify, before the normal-DB read / sidecar / isolation / Source creation / provider** (frozen order); durable content-free `preflight_failure.json` on block. 16 new 08D tests (incl. faithful attempt-#4 bad-context reproduction, the provider/sidecar/source-before-preflight=NO ordering guarantee, and fail-closed-on-any-resolver-error); 111 in the 08/08A/08B/08C/08D suite; GraphRAG regression (flag off) 522 pass / 8 skip / 0 fail; ruff + targeted mypy clean; fixture hash unchanged. **NOTE:** 08D permanently guards the attempt-#4 launcher/import defect — it does **not** imply attempt #5 was never run. Attempt #5 (`13e59a3edbb8`) subsequently ran with a corrected launcher and **reached genuine GraphRAG indexing**, empirically confirming the fix, before failing on S001 (TRACK / NON_RETRYABLE) — a real extraction failure, categorically different from the #4 launcher defect. **`FULL_EXECUTION_AUTHORIZED=NO`** (readiness ≠ authorization). **Eval-only; production imports no eval code; no migration (count 50); no `.env` edit; no provider traffic; sidecar stopped; `GRAPHRAG_ENABLED` false.** | [`GRAPHRAG_08D_LAUNCHER_IMPORT_PREFLIGHT.md`](GRAPHRAG_08D_LAUNCHER_IMPORT_PREFLIGHT.md) |
| GraphRAG-08E — Concurrent-indexing / load-interaction forensic + offline diagnostic harness | 🟨 **OFFLINE DIAGNOSTIC HARNESS COMPLETE — checkpointed; not a value run; not a root-cause confirmation; live diagnostic NOT run.** Follow-up to the S001 forensic (`S001_ISOLATED_RESULT=PASS` — S001 indexes cleanly alone; `DETERMINISTIC_SOURCE_SPECIFIC=NO`; `CONCURRENCY_LOAD_CAUSE=SUPPORTED`). Offline forensic (verified from source): `CONCURRENT_SUBMIT_PATTERN=SUBMIT_ALL_UP_FRONT`, `ON_SIDE_CONCURRENCY_LIMIT=NONE`, `SIDECAR_CONCURRENCY=LIGHTRAG_DEFAULT` — the full-run path hands all 75 docs to LightRAG before polling; the sidecar extracts at its default internal concurrency (many concurrent `gpt-4o-mini` calls). This **supports but does not prove** a load/concurrency interaction. Three hypotheses remain **UNCONFIRMED**: H1 (load-induced transient provider error whose wording misses the frozen retry allowlist), H2 (genuinely non-transient malformed/empty/parse failure under contention), H3 (LightRAG v1.5.6 concurrency/shared-state). New **eval-only** `concurrency_diag08.py`: bounded plan + caps (levels {1,2,4,8}, ≤4 levels, ≤8 Sources/level, ≤3 reps, ≤64 submissions; over-cap fails closed), deterministic S001-anchored synthetic selection (Sources only, no HOLDOUT), content-safe error-family taxonomy (diagnostic label only — never the retry decision), **decision-twin** reusing frozen `index_retry08.is_transient_reason`, ephemeral raw-error containment (never persisted), content-free records/aggregation, conservative H1/H2/H3 interpretation (SUPPORTED/WEAKLY_SUPPORTED/NOT_SUPPORTED/INCONCLUSIVE, `root_cause_confirmed` hard-wired False), and a **fail-closed** live seam (`run_sweep` requires `authorized_live=True` + active Option-A isolation + injected indexer — never called this phase) plus a `DiagnosticBudgetExceededError` guard. 42 08E tests; GraphRAG regression (flag off) **564 pass / 8 skip / 0 fail**; ruff + targeted mypy clean; independent adversarial review no HIGH/MEDIUM (2 LOW fixed+tested, 1 non-issue). **`ROOT_CAUSE_CONFIRMED=NO` · `LIVE_DIAGNOSTIC_EXECUTED=NO` · `FULL_RUN_ATTEMPT_6_EXECUTED=NO` · `GRAPH_RAG_08E_READY_FOR_LIVE_DIAGNOSTIC_AUTHORIZATION=YES` (eligibility only) · `FULL_EXECUTION_AUTHORIZED=NO` · `VALUE_EVIDENCE_READY=NO`.** No retry/allowlist/classifier/concurrency/fixture/provider/model/timeout change; production imports no eval code; no migration (count 50); no `.env` edit; no provider traffic; sidecar stopped; `GRAPHRAG_ENABLED` false. | [`GRAPHRAG_08E_CONCURRENT_INDEXING_FORENSIC.md`](GRAPHRAG_08E_CONCURRENT_INDEXING_FORENSIC.md) |
| GraphRAG-08E.1 — Diagnostic experimental-cell isolation hardening | 🟨 **OFFLINE DESIGN + IMPLEMENTATION COMPLETE — checkpointed; not a live run; not a root-cause confirmation.** The 08E bounded-live-diagnostic authorization was **BLOCKED before any provider traffic** (`LIVE_DIAGNOSTIC_AUTHORIZATION_ATTEMPT_1=BLOCKED_BEFORE_PROVIDER`, blocker `EXPERIMENTAL_CELL_ISOLATION_UNDEFINED`; provider/sidecar/DB = none; **produced zero diagnostic evidence** — not an H1/H2/H3 result): the committed `run_sweep` did not isolate LightRAG state between cells, and since the same synthetic Source content recurs across levels/reps, LightRAG's LLM response cache would let a later cell reuse a prior cell's extraction, invalidating failure-rate-vs-concurrency. Pinned v1.5.6 (`b33c6b0`) source forensic: a `workspace` scopes every store under `working_dir/<workspace>/` (incl. `kv_store_llm_response_cache.json`, `graph_*.graphml`, `vdb_*.json`); in-memory `shared_storage` keyed by `(namespace, workspace)`; the HTTP server workspace is fixed at startup ⇒ per-cell isolation needs a **fresh server process per cell**. **Strategy: OPTION B — fresh LightRAG process per cell + unique per-cell WORKSPACE.** New eval-only `cell_isolation08.py`: `CellIdentity`/unique workspace, `cross_cell_storage_isolated` path-disjointness proof, `CellRegistry` uniqueness guard, `diagnostic_cell08` context (validity gate + owned-only cleanup, fail-closed), and `run_sweep` now **requires** cell isolation (injected indexer runs only inside an entered valid cell; invalid cell fail-STOPS the sweep, no mid-run repair). `CROSS_CELL_LLM_CACHE_REUSE_POSSIBLE=NO`, `CROSS_CELL_GRAPH_STATE_REUSE_POSSIBLE=NO`, repetition/level independence PASS, same Source content preserved. 14 new 08E.1 tests + 42 updated 08E tests; GraphRAG regression (flag off) **578 pass / 8 skip / 0 fail**; ruff + targeted mypy clean; independent review no HIGH/MEDIUM (LOW-1 exception-chaining fixed+tested; LOW-2/LOW-3 = HARD requirements for 08E.2; LOW-4 defense-in-depth). **`CELL_ISOLATION_DESIGN_READY=YES` · `CELL_ISOLATION_CONTRACT_DEFINED=YES` · `CELL_PROVISIONER_IMPLEMENTED=NO` · `GRAPH_RAG_08E_READY_FOR_LIVE_DIAGNOSTIC_REAUTHORIZATION=NO` (blocker `LIVE_PER_CELL_PROVISIONER_NOT_IMPLEMENTED` — the live sidecar-restart-per-cell provisioner is the separate offline 08E.2 gate) · `ROOT_CAUSE_CONFIRMED=NO` · H1/H2/H3 UNCONFIRMED · `FULL_EXECUTION_AUTHORIZED=NO` · `VALUE_EVIDENCE_READY=NO`.** No plan/budget/retry/allowlist/classifier/concurrency/fixture/provider/model/LightRAG-version change; production imports no eval code; no migration (count 50); no `.env` edit; no provider traffic; sidecar stopped; `GRAPHRAG_ENABLED` false. | [`GRAPHRAG_08E1_CELL_ISOLATION_HARDENING.md`](GRAPHRAG_08E1_CELL_ISOLATION_HARDENING.md) |
| GraphRAG-08E.2 — Live diagnostic-cell provisioner | 🟨 **OFFLINE IMPLEMENTATION + TEST + INDEPENDENT REVIEW COMPLETE — execution plumbing only; awaiting operator checkpoint; NOT a live run; NOT a root-cause confirmation.** Implements the per-cell LightRAG provisioner that 08E.1 declared as its live blocker (`LIVE_PER_CELL_PROVISIONER_NOT_IMPLEMENTED`). New eval-only `cell_provisioner08.py` realizes the frozen `cell_isolation08.CellProvisioner` contract with a real process+storage lifecycle (atomic state machine `PLANNED→STORAGE_RESERVED→PROCESS_STARTED→HEALTH/VERSION/WORKSPACE_VERIFIED→PROVISIONED`; owned teardown `→PROCESS_STOPPED→STORAGE_DISPOSED→CLEANED`) plus a richer `ProvisionedCell` handle via `provision_diagnostic_cell`. **OPTION B storage model:** run-owned working dir `<eval_root>/<run_id>/` (never deleted by cell teardown), cell-owned workspace subdir `<working_dir>/<workspace>/` (the only path created/destroyed per cell). Discharges the two 08E.1 HARD review requirements: **LOW-2** atomic/self-cleaning provision (any post-reservation failure disposes partial resources + positively verifies, else `CellCleanupError` — never reports PROVISIONED over uncertain residue) and **LOW-3** physical freshness (`scan_workspace_freshness` inspects disk; pre-existing workspace fails closed; directory emptiness is primary, filename list is defense-in-depth). Fail-closed safety: path/ownership/port/version/workspace-runtime verification, guarded owned-only recursive delete (`is_within_root` realpath+case-norm, refuses symlink/junction escape), Windows `taskkill /T`→`/F /T` + POSIX `killpg` process-tree kill (real fake-child-tree test), idempotent double-cleanup, content-free logs (no credential/provider/Source text). Minimal `run_sweep` guard: a missing/invalid provisioner fails closed before the injected indexer (§35/§58); readiness never implies live authorization (§36/§59). Process/health primitives are **injected** — the offline suite starts NO real process, makes NO provider/network call (autouse httpx guard), mutates NO DB. **29 new 08E.2 tests (1 symlink test skipped — Windows privilege; guard covered at unit level); 148 pass across 08E.2+08E.1+08E+08A–08D; GraphRAG regression (flag off) 607 pass / 9 skip / 0 fail; ruff clean; targeted mypy clean (`cell_provisioner08` clean; `concurrency_diag08` delta clean — 5 pre-existing `object`-attr errors untouched, NOT whole-repo clean); independent adversarial review PASS — 0 HIGH; 1 MEDIUM (M1 rollback-coverage limited to `CellProvisionError`) FIXED before checkpoint (`except BaseException` + probe/makedirs wrapping) and pinned by new raising-prober + raising-non-provision-allocator tests; L1 timeout floor/message FIXED; L3 `_teardown` raw-escape FIXED+TESTED; L4 PID-reuse = documented residual LOW (utility not wired to recovery; live path targets container identity); L2 workspace-unverifiable live default = intentional fail-closed.** `REAL_LOCAL_SIDECAR_PROVISION_TEST=SKIPPED_SAFETY` (offline proof strong; §32/§75 preconditions not established). **`CELL_PROVISIONER_IMPLEMENTED=YES` · `GRAPH_RAG_08E_READY_FOR_LIVE_DIAGNOSTIC_REAUTHORIZATION=YES` (eligibility only — a separate operator prompt is required) · `LIVE_DIAGNOSTIC_AUTHORIZED=NO` · `LIVE_DIAGNOSTIC_EXECUTED=NO` · `FULL_RUN_ATTEMPT_6_EXECUTED=NO` · `ROOT_CAUSE_CONFIRMED=NO` · H1/H2/H3 UNCONFIRMED · `FULL_EXECUTION_AUTHORIZED=NO` · `VALUE_EVIDENCE_READY=NO`.** No plan/budget/retry/allowlist/classifier/concurrency/fixture/provider/model/LightRAG-version change; production imports no eval code; no migration (count 50); no `.env` edit; no provider traffic; sidecar not started; `GRAPHRAG_ENABLED` false. | [`GRAPHRAG_08E2_LIVE_CELL_PROVISIONER.md`](GRAPHRAG_08E2_LIVE_CELL_PROVISIONER.md) |
| GraphRAG-08E — Live Concurrency Diagnostic Reauthorization #2 | 🟥 **BLOCKED AT STAGE A — provider-free real-sidecar smoke FAILED fail-closed; Stage B NOT run; PROVIDER_TRAFFIC=NO; produced ZERO diagnostic evidence (not H1/H2/H3, not a provider/indexing failure).** The committed 08E.2 provisioner was run (real Docker + health) against a real `ghcr.io/hkuds/lightrag:v1.5.6` container, provider-free (no LLM/embed bindings, no index/query). Container started + became healthy, then the provisioner correctly **failed closed** — surfacing two committed live-path attestation defects the offline fakes had masked: (A) **version-string mismatch** — /health reports `1.5.6`, `expected_version` default is `v1.5.6` → `CellVersionMismatchError`; (B) **runtime workspace unverifiable** — committed `HealthResult` had no workspace field. Atomic rollback on the **real** container = PASS (`stopped/port_released/storage_absent`, zero residue); fixture unchanged; no git ops. `LIVE_DIAGNOSTIC_EXECUTED=NO · DEV/HOLDOUT=0/0 · ATTEMPT_6=NOT_RUN · ROOT_CAUSE_CONFIRMED=NO · H1/H2/H3 UNCONFIRMED`. Next step **D — offline provisioner fix (→ 08E.3)**. | [`GRAPHRAG_08E3_REAL_SIDECAR_ATTESTATION_HARDENING.md`](GRAPHRAG_08E3_REAL_SIDECAR_ATTESTATION_HARDENING.md) |
| GraphRAG-08E.3 — Real-sidecar attestation hardening | 🟨 **OFFLINE FIX + PROVIDER-FREE REAL-SIDECAR SMOKE + TESTS + INDEPENDENT REVIEW COMPLETE — attestation semantics only; awaiting operator checkpoint; NOT a live diagnostic; NOT Stage B.** Fixes the two Stage-A defects. **Defect A:** `versions_equivalent` canonicalizes via the trusted `packaging.version.parse` — `v1.5.6` ≡ `1.5.6`, but `1.5.5/1.5.7/2.0.0/1.5` and malformed/absent forms fail closed (NOT permissive); `_verify_health` uses it instead of `==`. **Defect B:** forensic on the real v1.5.6 corrected the 08E.2 `L2` assumption — `/health` **does** expose the bound workspace via `configuration.workspace` (+ `working_directory`), the shared client had merely discarded it. New evidence-based attestation: **DIRECT** (`parse_health_payload` extracts ONLY workspace/working_directory/version/status from a raw content-safe /health — binding hosts discarded) AND/OR **DERIVED** (injected `CellRuntimeAttestor`; live `DockerRuntimeAttestor` inspects ONLY the owned container with targeted templates emitting ONLY the `WORKSPACE` env value + the rag-storage mount — never full `Config.Env`, so no provider key leaks), giving ownership (`container_identity==handle`) + host storage-mount attestation (`_paths_equal` to the owned run root). At least one evidence source required; present-but-mismatched or missing evidence fails closed; `require_runtime_workspace=True` never silently relaxed; failure cleans up with no handle. **Real provider-free smoke: PASS** — `PROVISIONED`, `version=1.5.6`, `PIN_EQUIVALENCE=PASS`, `WORKSPACE_DIRECT_RUNTIME_REPORT=YES`, `WORKSPACE_ATTESTATION=DIRECT_HEALTH_CONFIG+DERIVED_OWNED_CONTAINER_CONFIG`, storage/process/port ownership PASS, cleanup PASS, residue 0, provider traffic 0, indexing 0. **18 new 08E.3 tests + 08E.2 fake updated to the real `1.5.6` form; 103 pass across 08E.3+08E.2+08E.1+08E; GraphRAG regression (flag off) 625 pass / 9 skip / 0 fail; ruff clean; targeted mypy clean.** No change to diagnostic treatment/Source content/provider/model/retry/allowlist/full-run concurrency/LightRAG pin/fixture; production imports no eval code; no migration (count 50); no `.env` edit; sidecar not left running; `GRAPHRAG_ENABLED` false. **`GRAPH_RAG_08E_READY_FOR_LIVE_DIAGNOSTIC_REAUTHORIZATION=YES` (eligibility only — separate operator prompt required) · `LIVE_DIAGNOSTIC_AUTHORIZED=NO` · `LIVE_DIAGNOSTIC_EXECUTED=NO` · `ATTEMPT_6=NOT_RUN` · `ROOT_CAUSE_CONFIRMED=NO` · H1/H2/H3 UNCONFIRMED · `FULL_EXECUTION_AUTHORIZED=NO` · `VALUE_EVIDENCE_READY=NO`.** | [`GRAPHRAG_08E3_REAL_SIDECAR_ATTESTATION_HARDENING.md`](GRAPHRAG_08E3_REAL_SIDECAR_ATTESTATION_HARDENING.md) |
| GraphRAG-08E — Live Concurrency Diagnostic Reauthorization #3 | 🟥 **BLOCKED PRE-PROVIDER — the committed harness had NO live execution path; Stage B NOT run; PROVIDER_TRAFFIC=NO; ZERO diagnostic evidence.** Preflight passed on the 08E.3 checkpoint (fixture/tree/env clean) but the "08E harness exact approved version" check failed: `run_sweep` requires an injected `index_cell_fn` supplied only by fake/test code; no live orchestrator existed; the `DiagnosticCell` handed to the indexer carried no `base_url`/`port`. Executing would require writing new provider-backed code, which §11/§35 of the execution gate forbid. No git ops, tree CLEAN, HEAD unchanged. `CELLS_COMPLETED=0 · ACTUAL_SUBMISSIONS=0 · DEV/HOLDOUT=0/0 · ATTEMPT_6=NOT_RUN · ROOT_CAUSE_CONFIRMED=NO · H1/H2/H3 UNCONFIRMED`. Next step **C — build the live wiring offline (→ 08E.4)**. | [`GRAPHRAG_08E4_LIVE_DIAGNOSTIC_EXECUTION_WIRING.md`](GRAPHRAG_08E4_LIVE_DIAGNOSTIC_EXECUTION_WIRING.md) |
| GraphRAG-08E.4 — Live diagnostic execution wiring | 🟨 **OFFLINE IMPLEMENTATION + TESTS + INDEPENDENT REVIEW COMPLETE — execution wiring only; awaiting operator checkpoint; NOT a live diagnostic; NOT Stage B; NO provider traffic.** Implements the missing execution path Reauth #3 exposed. New eval-only `live_indexer08.py` (`LiveCellIndexer08`: indexes one valid provisioned cell's frozen Source subset against THAT cell's own sidecar → content-free `AttemptRecord`s; injected LightRAG client, frozen retry/taxonomy reuse, ownership-bound loopback endpoint, raw-error containment, cancellation propagates, ≤2 attempts/source) and `live_orchestrator08.py` (`LiveDiagnosticOrchestrator08`: preflight → **DockerRuntimeAttestor required** → **authorized_live deny-by-default** → Option-A isolation → temp Model → frozen Source prep + canonical embedding → per-cell provisioner+attestor → `run_sweep(authorized_live=True, require_isolation=True)` → content-free artifact → global cleanup; every provider/DB/Docker seam INJECTED; `default_live_deps` wires the real seams). One **read-only** `cell_provisioner08.active_provisioned_cell` accessor bridges the cell endpoint (ownership-bound; starts/indexes nothing — §22 boundary preserved). Double authorization gate (orchestrator `authorized_live` AND `run_sweep` `authorized_live` AND provisioned+valid cell); missing attestor / `authorized_live=False` / invalid cell / un-entered isolation each yield ZERO provider/index calls. Frozen reuse: `index_retry08.is_transient_reason` via `characterize_failure` (zero divergence), 08E taxonomy, `default_plan` (30 expected / 64 cap; worst legal 30×2=60≤64), `AttemptRecord` schema. Independent adversarial review PASS after fixes — safety envelope solid; two real live-path execution-validity defects FIXED: **H1** (real client now surfaces the FAILED doc's raw error text via the frozen `_fetch_failed_reason_ex` — else the sweep would be diagnostically void) and **M1** (client/prober API key defaults from `GRAPHRAG_POC_API_KEY`, matching the container); L1 documented (intentional single-classifier), L2 test-grep broadened. **27 new 08E.4 tests; 130 pass across 08E.4+08E.3+08E.2+08E.1+08E; GraphRAG regression (flag off) 652 pass / 9 skip / 0 fail; ruff clean; targeted mypy clean (new files clean; 5 pre-existing `concurrency_diag08` `object`-attr errors untouched, NOT whole-repo clean).** No provider traffic, no sidecar start, no DB mutation (all seams injected; import starts nothing); no change to Source lifecycle/embedding semantics/retry/allowlist/full-run concurrency/provider/model/LightRAG pin/fixture; production imports no eval code; no migration (count 50); no `.env`; no V/GQ/GD; attempt #6 unreachable. **`GRAPH_RAG_08E_READY_FOR_LIVE_DIAGNOSTIC_REAUTHORIZATION=YES` (execution path now COMPLETE — but a separate operator prompt + real `DockerRuntimeAttestor`+`default_live_deps` are required) · `LIVE_DIAGNOSTIC_AUTHORIZED=NO` · `LIVE_DIAGNOSTIC_EXECUTED=NO` · `ATTEMPT_6=NOT_RUN` · `ROOT_CAUSE_CONFIRMED=NO` · H1/H2/H3 UNCONFIRMED · `FULL_EXECUTION_AUTHORIZED=NO` · `VALUE_EVIDENCE_READY=NO`.** | [`GRAPHRAG_08E4_LIVE_DIAGNOSTIC_EXECUTION_WIRING.md`](GRAPHRAG_08E4_LIVE_DIAGNOSTIC_EXECUTION_WIRING.md) |
| GraphRAG-08E — Live Concurrency Diagnostic Reauthorization #4 | 🟥 **BLOCKED PRE-PROVIDER at the complete-wiring precheck; Stage B NOT run; PROVIDER_TRAFFIC=NONE; ZERO cells/containers/evidence.** Execution-only authorization. Checkpoint/fixture/env verified clean, but §7 failed: the committed `DockerCellProcessController.start` started fresh cell containers WITHOUT the OpenRouter LLM/embedding bindings (`LLM_BINDING`/`LLM_MODEL`/`LLM_BINDING_HOST`/`LLM_BINDING_API_KEY` + `EMBEDDING_*`) — they live only in the compose file the `docker run` path doesn't use — so extraction would fall back to the image's Ollama-localhost default and fail uniformly, reflecting the harness not concurrency. Completing that arrow needs new code, which the turn's ABSOLUTE RULE forbids. No git ops, tree CLEAN, HEAD unchanged. `CELLS_COMPLETED=0 · DEV/HOLDOUT=0/0 · ATTEMPT_6=NOT_RUN · H1/H2/H3 UNCONFIRMED`. Next step **C — offline fix (→ 08E.5)**. | [`GRAPHRAG_08E5_PROVIDER_BINDING_INJECTION_HARDENING.md`](GRAPHRAG_08E5_PROVIDER_BINDING_INJECTION_HARDENING.md) |
| GraphRAG-08E.5 — Live cell provider-binding injection hardening | 🟨 **OFFLINE IMPLEMENTATION + TESTS + INDEPENDENT REVIEW COMPLETE — binding transport/attestation only; awaiting operator checkpoint; NOT a live diagnostic; NOT Stage B; NO provider traffic.** Fixes the Reauth #4 blocker. New eval-only `provider_binding08.py` (`DiagnosticProviderBinding08` + `frozen_provider_binding()`: OpenRouter, LLM `openai/gpt-4o-mini`, embedding `openai/text-embedding-3-small` dim 1536, host `https://openrouter.ai/api/v1`; carries only public config + secret env **NAMES**, never a value; `validate()` rejects drift — not a model-selector). `DockerCellProcessController.start` injects it: **public** binding values on argv, **secret** values by env-INHERITANCE (bare `-e NAME` + `subprocess.run(env=…)`, never `-e NAME=value`) — the pre-existing `LIGHTRAG_API_KEY` argv placement fixed the same way, so no key ever reaches argv/repr/logs/`AttemptRecord`/artifact. Runtime attestation extended: `DockerRuntimeAttestor` reads the 4 PUBLIC binding values via a whitelist template (never the `*_API_KEY` vars) + provider-secret PRESENCE (`PRESENT` sentinel, never the value); the `_attest_provider_binding` gate (behind `require_provider_binding`) proves the owned container is bound to the frozen models AND both secrets present, else `CellProviderBindingError` (no Ollama fallback). Two secret boundaries kept separate (sidecar `GRAPHRAG_POC_API_KEY` vs external `OPENROUTER_API_KEY`). Orchestrator/`default_live_deps` require+validate the binding, fail fast content-safe on a missing secret (`REQUIRED_RUNTIME_SECRET_MISSING=<name>`), and set `require_provider_binding=True` so every cell gets the identical frozen binding; binding presence never implies authorization. Independent review PASS (0 HIGH; MEDIUM-1 `EMBEDDING_DIM`-not-transported documented as intentional compose-parity; LOW-1 host attestation FIXED+tested; LOW-3 binding-gate ownership/liveness FIXED+tested; LOW-2 double-inspect accepted). **21 new 08E.5 tests + 08E.4 deps updated to the 3-arg factory+required binding; 151 pass across 08E.5+08E.4+08E.3+08E.2+08E.1+08E; GraphRAG regression (flag off) 673 pass / 9 skip / 0 fail; ruff clean; targeted mypy clean (new files clean; 5 pre-existing `concurrency_diag08` errors untouched, NOT whole-repo clean).** No provider traffic, no container start, no DB mutation; no change to Source content/cell plan/concurrency/retry/allowlist/provider/model/LightRAG pin/fixture; production imports no eval code; no migration (count 50); no `.env`; no secret value stored/logged. **Execution completeness (§72): a future authorized run needs NO new code / NO manual public-provider wiring — only operator authorization + runtime secrets. `GRAPH_RAG_08E_READY_FOR_LIVE_DIAGNOSTIC_REAUTHORIZATION=YES` (separate operator prompt required) · `LIVE_DIAGNOSTIC_AUTHORIZED=NO` · `LIVE_DIAGNOSTIC_EXECUTED=NO` · `ATTEMPT_6=NOT_RUN` · `ROOT_CAUSE_CONFIRMED=NO` · H1/H2/H3 UNCONFIRMED · `FULL_EXECUTION_AUTHORIZED=NO` · `VALUE_EVIDENCE_READY=NO`.** | [`GRAPHRAG_08E5_PROVIDER_BINDING_INJECTION_HARDENING.md`](GRAPHRAG_08E5_PROVIDER_BINDING_INJECTION_HARDENING.md) |
| GraphRAG-08E — Live Concurrency Diagnostic Reauthorization #5 | 🟥 **BLOCKED PRE-PROVIDER — required runtime secret missing; STOPPED before any provider traffic; ZERO cells/containers/evidence.** Execution-only authorization on the 08E.5 checkpoint. Checkpoint/fixture/env verified; committed entrypoint fully resolved (`LIVE_ENTRYPOINT_COMPLETE=YES`); but the §6 required-secret gate found `OPENROUTER_API_KEY` absent → fail-closed by design (`REQUIRED_RUNTIME_SECRET_MISSING=OPENROUTER_API_KEY`). **NOT a harness defect and NOT next-step C** — the committed fail-closed behavior worked exactly as designed. No git ops, tree CLEAN, HEAD unchanged. `PROVIDER_TRAFFIC=NONE · CELLS_COMPLETED=0 · ACTUAL_SUBMISSIONS=0 · ATTEMPT_6=NOT_RUN · H1/H2/H3 UNCONFIRMED`. Resolution: operator sets the secret (synthetic-data-approved OpenRouter credential) + re-authorizes. | eval-only `live_orchestrator08.py` |
| GraphRAG-08E — Live Concurrency Diagnostic Reauthorization #5-R1 | ✅ **COMPLETE — the bounded live diagnostic RAN end-to-end; 8/8 cells; 30/30 submissions PROCESSED; 0 failures; 0 retries; NOT a value run; NOT a root-cause confirmation.** Retry of #5 after the operator provided `OPENROUTER_API_KEY` (`.env` supplied Surreal creds + encryption key + `GRAPHRAG_POC_API_KEY`). Committed **no-glue** entrypoint (`load_benchmark08()` + `LiveDiagnosticOrchestrator08(bench, default_live_deps(eval_root=…)).run(authorized_live=True)`); no code/fakes/manual provider wiring. Levels [1,2,4,8]×2 into **fresh per-cell v1.5.6 sidecars**; every cell version+workspace+storage+provider-binding attested; per-level failure_rate 0.0; latency higher-tail at L4–L8 (median 15305/12866/15717.5/21616 ms; max 15313/15422/25765/36921) — **not strictly monotonic** (L2 median dips as faster non-anchor Sources enter); **S001 PROCESSED in every treatment → failure NOT reproduced**. `interpret_hypotheses`: H1/H2/H3 **INCONCLUSIVE**, `root_cause_confirmed=false` ("insufficient data — needs failures across ≥2 levels"). Full cleanup: no residual `graphrag_eval_*` namespaces, temp Model gone, normal DB unchanged (1→1), all containers absent, port free; fixture `a58a6853…143d` unchanged; tree CLEAN, HEAD unchanged. Content-safe (`PROVIDER_TRAFFIC=BOUNDED_DIAGNOSTIC_ONLY`, `RAW_ERROR_PERSISTED=NO`, `SECRET_VALUE_REPORTED=NO`). `ROOT_CAUSE_CONFIRMED=NO · ATTEMPT_6=NOT_RUN · FULL_EXECUTION_AUTHORIZED=NO · VALUE_EVIDENCE_READY=NO`. Next step **B — DIAGNOSTIC_INCONCLUSIVE_REQUIRES_REVIEW (→ 08E.6)**. | [`GRAPHRAG_08E6_SCALE_ACCUMULATION_FORENSIC.md`](GRAPHRAG_08E6_SCALE_ACCUMULATION_FORENSIC.md) |
| GraphRAG-08E.6 — Full-run scale/accumulation reproduction forensic & diagnostic-design gate | 🟨 **OFFLINE FORENSIC + DESIGN ONLY — docs only; awaiting operator review; not a live run; not a root-cause confirmation.** Determines what execution dimensions existed in full-run attempt #5 but were NOT exercised by the clean #5-R1. Source-traced reconstruction: attempt #5 = **ONE long-lived sidecar**, **submit-all-75-up-front**, **no ON-side concurrency limit** (runner08.py:490–494; precheck08.py:568/683), one shared workspace/accumulating store, LightRAG default internal extraction concurrency; S001 submitted first, failed at TRACK NON_RETRYABLE with **`graphrag_indexed_count=0`** (no source PROCESSED at abort — failure under the 75-way concurrent in-flight burst, not after cumulative *completed* work). #5-R1 = 8 fresh cells, `asyncio.gather` ≤8-way (`concurrency==source_count`), fresh state per cell. Five axes separated (instantaneous concurrency / cumulative load / state accumulation / burstiness / process lifetime): all confounded in #5 and all reset in #5-R1 — and in #5-R1 A==B==D by construction, so **neither run isolates any axis**. `PRIMARY_DIFFERENCE_AXIS = E (multiple confounded); untested regime = the higher-scale confounded burst (8-way clean vs ~72-way unexercised)`; cumulative-*completed*-scale weakly implicated (0 completed at failure), in-flight state (C) + submitted-scale (B) remain fully in play. Clean #5-R1 is plausibly explained by per-cell fresh-state resets (review Q-G = YES). `S001_FAILURE_REPRODUCED=NO · H1/H2/H3 INCONCLUSIVE · ROOT_CAUSE_CONFIRMED=NO · 08F_MITIGATION_GATE_JUSTIFIED=NO · FULL_ATTEMPT_6_JUSTIFIED_NOW=NOT_YET · NEXT_DIAGNOSTIC_REQUIRED=YES`. Proposed **GraphRAG-08E.7 (design only, NOT built)** — single-sidecar **burst-REPRODUCTION** sweep scaling one concurrent wave 8→~72 into a fresh sidecar (predeclared S001-anchored prefix, stop-on-first-reproduction, bounded budget, NO DEV/HOLDOUT/V/GQ/GD); it reproduces (does not isolate) the confounded regime, with axis separation deferred to an optional secondary sequential arm. `NEXT_DIAGNOSTIC_REQUIRES_NEW_CODE=YES` (committed caps levels {1,2,4,8}/≤8 sources/≤64 total + `select_diagnostic_sources` cap + fresh-sidecar-per-cell; execution machinery reused; higher caps are a diagnostic-instrument bound, not a frozen-policy change). Independent adversarial review of the eight §26 questions: no HIGH; 3 MEDIUM + 3 LOW (framing/precision) resolved. No code change (docs only); no migration (count 50); zero provider traffic; sidecar not started; `GRAPHRAG_ENABLED` untouched; fixture `a58a6853…143d` unchanged. `FULL_EXECUTION_AUTHORIZED=NO · VALUE_EVIDENCE_READY=NO`. | [`GRAPHRAG_08E6_SCALE_ACCUMULATION_FORENSIC.md`](GRAPHRAG_08E6_SCALE_ACCUMULATION_FORENSIC.md) |
| GraphRAG-08E.7A — Single-sidecar burst-reproduction experimental design freeze | 🟨 **OFFLINE FORENSIC + DESIGN-FREEZE ONLY — docs only; awaiting operator review; NOT implemented; NOT authorized to run.** Freezes an exact bounded 08E.7 SCREENING reproduction sweep before any code/live authorization. **Attempt-#5 source order PROVEN** (corpus.json ascending S001…S075 = full-run submit order runner08.py:490; S001 first). **Failure signature reconstructed from committed code+docs:** TRACK-surface `DocStatus.FAILED`, `TRACK_TEXT_PRESENT_NO_ALLOWLIST_MATCH` (index_retry08.py:125), NON_RETRYABLE/`retry_allowed=false`, error-text present, bucket `129_256`, empty match classes, family UNKNOWN, S001 attempt_count=1, raw text never persisted. Frozen design: `BURST_LEVELS=[8,16,24,32,48,75]`, `TOP_RUNG=75` (exact full-corpus burst, not ~72; approached progressively), `REPETITIONS_PER_LEVEL=1` (screening), deterministic **nested-prefix** selection (first C of S001…S075, S001 fixed first, no cherry-pick/dup), immutable fixture. Budgets distinguished exactly: `MAX_PLANNED_SOURCE_WORKLOAD=203` (8+16+24+32+48+75) vs `MAX_INDEX_ATTEMPTS_TOTAL=406` (203×MAX_INDEX_ATTEMPTS_PER_SOURCE=2) — harness counts attempts (live_indexer08.py:277), retries count toward 406 not 203. `STOP_ON_FIRST_VALID_FAILURE=YES` → classify A (historical-signature reproduced, exact `retry_reason_code` match, length-bucket/family excluded) vs B (novel valid failure) → STOP (confirmation is a separate gate). No-failure through 75 ⇒ `HISTORICAL_FAILURE_REPRODUCED=NO` (does NOT disprove H1/H2/H3). Reproduction ⇒ `ROOT_CAUSE_CONFIRMED=NO` (reproduction ≠ axis isolation). Frozen provider/models/version/retry unchanged. `08E7_IMPLEMENTATION_REQUIRES_NEW_CODE=YES` (new higher-cap plan + stop-on-first-valid-failure + raised selection cap + attempt cap + optional submit-all-then-poll wave; live STACK — Option-A/binding/attestor/provisioner/LiveCellIndexer08/cleanup — fully reused). Independent adversarial review of the ten §41 questions: no HIGH; 3 MEDIUM + 2 LOW resolved (classifier-signature ≠ mechanism; expected clean-path cost is the full 203 submissions ≈ 2.7× a single 75-index; novel-failure stop leaves rungs above C unobserved). **Precision addendum (tightened before checkpoint):** `BURST_SCHEDULING_MODE=SUBMIT_ALL_THEN_POLL` frozen (required; gather-per-source not acceptable); stop rule is a **ladder** stop with current-rung bounded **drain** (no in-flight cancellation) + full-initial-wave treatment-validity (partial wave ⇒ TREATMENT_VALID=NO); reproduction flag **split** into `CLASSIFIER_SIGNATURE_REPRODUCED` + strict `S001_HISTORICAL_EVENT_REPRODUCED_STRICT` (+ separate `HISTORICAL_ATTEMPT_NUMBER_MATCH`) with non-exclusive rung classification; `MAX_PLANNED_SOURCE_WORKLOAD=203` and `MAX_INDEX_ATTEMPTS_TOTAL=406` both mechanically guarded. `08E7_LIVE_AUTHORIZED=NO · FULL_ATTEMPT_6_AUTHORIZED=NO · 08F_JUSTIFIED=NO · FULL_EXECUTION_AUTHORIZED=NO · VALUE_EVIDENCE_READY=NO`. No code (docs only); no migration (count 50); zero provider traffic; sidecar not started; `GRAPHRAG_ENABLED` untouched; fixture unchanged. | [`GRAPHRAG_08E7A_BURST_REPRODUCTION_DESIGN.md`](GRAPHRAG_08E7A_BURST_REPRODUCTION_DESIGN.md) |
| GraphRAG-08E.7B — Burst-reproduction harness implementation | 🟨 **OFFLINE IMPLEMENTATION + TESTS + INDEPENDENT REVIEW — execution plumbing only; awaiting operator checkpoint; NOT a live run; NO provider traffic; NOT a reproduction result.** Implements the frozen 08E.7A screening design. New eval-only `burst_plan08.py` (frozen `BurstPlan`/`validate_burst_plan` fail-closed to the exact `[8,16,24,32,48,75]` ladder; `select_burst_prefix` nested S001-first prefixes; `BurstBudgetGuard` — DISTINCT 203 planned-workload vs 406 index-attempt hard caps + per-Source 2-cap; split reproduction predicates + `classify_rung`/result types) and `burst_runner08.py` (`BurstWaveIndexer08` = true **SUBMIT_ALL_THEN_POLL** wave — submits all C initial requests before ANY poll, gather-per-source explicitly rejected; `BurstLadderRunner08` = fresh sidecar per rung via `diagnostic_cell08`, full-initial-wave validity, current-rung bounded **drain** with no in-flight cancellation, ladder-stop after first valid-failure rung; `BurstReproductionOrchestrator08` deny-by-default, **reuses `OrchestratorDeps`/`default_live_deps`** so a future live run needs NO new code). Split classification: `CLASSIFIER_SIGNATURE_REPRODUCED` (broad) vs strict `S001_HISTORICAL_EVENT_REPRODUCED_STRICT` (+ separate `HISTORICAL_ATTEMPT_NUMBER_MATCH`) vs `NOVEL_VALID_FAILURE`, non-exclusive rung labels; a signature match never implies the S001 event or a root cause (`root_cause_confirmed` hard-wired False). Harness/runtime failures (partial wave, attestation/cleanup/budget/cancellation) are runtime stops, never experimental evidence. Raw error text consumed transiently only (never persisted); results carry logical Source IDs + safe reason codes only. 08E `[1,2,4,8]×2` caps in `concurrency_diag08` untouched. **36 new 08E.7B tests; 187 pass / 1 skip across 08E.7B+08E.5+08E.4+08E.3+08E.2+08E.1+08E; GraphRAG regression (flag off) 709 pass / 9 skip / 0 fail; ruff clean; targeted mypy clean (both new files; 5 pre-existing `concurrency_diag08` errors untouched, NOT whole-repo clean).** No provider traffic; no container start; no DB mutation; production imports no eval code; no migration (count 50); no `.env`; fixture `a58a6853…143d` unchanged. `08E7_IMPLEMENTATION_REQUIRES_NEW_CODE=YES (satisfied) · FUTURE_LIVE_RUN_REQUIRES_NEW_CODE=NO · 08E7_LIVE_AUTHORIZED=NO · FULL_ATTEMPT_6_AUTHORIZED=NO · 08F_JUSTIFIED=NO · ROOT_CAUSE_CONFIRMED=NO · H1/H2/H3 INCONCLUSIVE · HISTORICAL_SIGNATURE_REPRODUCED=NOT_RUN · VALUE_EVIDENCE_READY=NO`. | [`GRAPHRAG_08E7B_BURST_HARNESS_IMPLEMENTATION.md`](GRAPHRAG_08E7B_BURST_HARNESS_IMPLEMENTATION.md) |
| GraphRAG-08E.7 — Live single-sidecar burst-reproduction Authorization #1 | ✅ **COMPLETE — live screening sweep RAN clean through C=75; historical failure NOT reproduced; NOT a value run; NOT a root-cause confirmation.** Executed the committed 08E.7B harness (run id `gr08e7a1`, HEAD 2858a11 / tag graphrag-08e7b-burst-harness-approved) via the no-glue entrypoint `BurstReproductionOrchestrator08(load_benchmark08(), default_burst_deps(eval_root=…)).run(authorized_live=True)`; `.env` supplied Surreal creds + encryption key + GRAPHRAG_POC_API_KEY, OPENROUTER_API_KEY present. **All 6 rungs [8,16,24,32,48,75] entered, every one CLEAN** (8/8·16/16·24/24·32/32·48/48·75/75 PROCESSED; full initial wave + valid + drained + cleaned up each); **0 failures, 0 retries; workload 203/203, index attempts 203/406.** `first_failure_rung=NONE`, `NO_FAILURE_THROUGH_BURST_LEVEL=75`. `HISTORICAL_SIGNATURE_REPRODUCED=NO · S001_HISTORICAL_EVENT_REPRODUCED_STRICT=NO · NOVEL_VALID_FAILURE=NO · ROOT_CAUSE_CONFIRMED=NO`. The attempt-#5 S001 TRACK failure did NOT reproduce even at the full 75-source burst into one long-lived sidecar (the exact regime that failed at attempt #5) ⇒ `HISTORICAL_FAILURE_DETERMINISTIC_ON_75_WAY_BURST=NOT_SUPPORTED` (does NOT disprove the historical failure or H1/H2/H3). `BURST_LATENCY_PRESSURE=STRONG_AND_SCALE_CORRELATED` (per-rung median ms 21976→41617→57694→69062→93365→142687; max 30937→…→277061) — performance evidence only, failure_rate 0 at every rung. H1/H2/H3 INCONCLUSIVE (no failure to characterize). **Safety PASS:** Option-A isolation, NORMAL_DB_UNCHANGED, DockerRuntimeAttestor used, fresh per-rung sidecar/workspace/storage, no cross-rung cache/graph reuse, full cleanup (no residual namespaces/model/containers/workspaces, port free), GRAPHRAG_ENABLED false, fixture `a58a6853…143d` unchanged; content-safe (PROVIDER_TRAFFIC=BOUNDED_BURST_DIAGNOSTIC_ONLY: 75 canonical embeddings + 203 indexing submissions; provider-internal calls NOT_OBSERVABLE; RAW_ERROR_PERSISTED=NO, SECRET_VALUE_REPORTED=NO). Tree CLEAN, HEAD unchanged; no git ops during the run. `08F_MITIGATION_GATE_JUSTIFIED=NO`. **`FULL_ATTEMPT_6_JUSTIFIED_NOW` updated NOT_YET → YES** (the previously-blocking full 75-source indexing burst rehearsed successfully under the current stack) **but `FULL_ATTEMPT_6_AUTHORIZED=NO`** (separate authorization required; root cause NOT solved — a future run must still fail closed). Retained: `VALUE_EVIDENCE_READY=NO`, all `*_VALUE_EVIDENCED=NOT_RUN`, `FULL_RUN_ATTEMPT_6_EXECUTED=NO`. Next step **C — NO_REPRODUCTION_THROUGH_75_REQUIRES_REVIEW**. | [`GRAPHRAG_08E7C_LIVE_BURST_REPRODUCTION_RESULT.md`](GRAPHRAG_08E7C_LIVE_BURST_REPRODUCTION_RESULT.md) |
| GraphRAG-08 — Full benchmark Attempt #6 (value-evidence run) | ✅ **COMPLETE — the FIRST fully valid larger-corpus DEV/HOLDOUT value benchmark; NOT a positive graph value; NOT a root-cause confirmation.** Executed the canonical committed full-run path `precheck08.run_full_benchmark(authorization_label="FULL_RUN_ATTEMPT_6")` (run id `gr08-3bdb09b4dd1e`, HEAD bc5964f). **INDEXING 75/75 on first attempt, 0 retries, 0 failures** (max_attempts_observed=1) — the attempt-#1..#5 blocker resolved (does NOT solve root cause). All 60 queries (30 DEV + 30 HOLDOUT) evaluated across V (vector), GQ (`client.query`), GD (`/query/data`). **Vector (ranked, HOLDOUT authoritative):** Hit@1 0.000 / @3 0.417 / @5 0.667 / @10 0.958; Recall@5 0.521 / @10 0.875; MRR 0.267; FullSet@10 0.750. **GQ==GD IDENTICAL Source sets on all 60** (gq_eq_gd=60, gq_only=0, gd_only=0 — confirms GraphRAG-06 parity): fixed broad **20/75 candidate surface** (candidate_fraction 0.267) → set_recall ≈0.97, full_source_set_recovered 0.917 BUT **set_precision 0.065, F1 0.12, ~18.5 FP/query, negatives abstained_rate 0.0** (returns 20 cands for all 12 unanswerable). Provenance clean (0 foreign/malformed/off-benchmark). **Latency:** GD p50 968ms vs GQ p50 6344ms (~6.5× faster, same evidence, avoids the discarded final-answer LLM) vs V 1109ms. **Decisions: `VALUE_EVIDENCE_READY=YES`; `GRAPH_RETRIEVAL_VALUE_EVIDENCED=NO`** (high recall is broad-surface coverage, not precision — does not beat ranked vector; §40); **`STRUCTURED_QUERY_DATA_RUNTIME_VALUE_EVIDENCED=YES`** (GD = GQ evidence, ~6.5× faster, no final-answer LLM — runtime value, NOT retrieval value). `COMPLEMENTARITY_PRESENT=YES` but `COMPLEMENTARITY_DEMONSTRATES_USEFUL_GRAPH_RETRIEVAL=NO` (coverage artifact of the broad surface; oracle union is upper-bound only, NOT RRF/fusion). Retained: `RRF_CANDIDATE_INTERFACE_READY=NO`, `GRAPH_CANDIDATE_IMPLEMENTATION_READY=NO`, `QUERY_DATA_EXPOSES_VALID_RANK/SCORE=NO`, `STRUCTURED_EVIDENCE_IMPLEMENTATION_READY=NO`, `08F_MITIGATION_GATE_JUSTIFIED=NO`, `ROOT_CAUSE_CONFIRMED=NO`, H1/H2/H3 INCONCLUSIVE. **Safety PASS:** Option-A isolation, NORMAL_DB_UNCHANGED (1→1), sidecar stopped, temp model + LightRAG cleaned, no residual namespaces/containers, embedding dim 1536, GRAPHRAG_ENABLED false, fixture `a58a6853…143d` unchanged; content-safe (FULL_FROZEN_BENCHMARK_ONLY; RAW_ERROR_PERSISTED=NO, SECRET_VALUE_REPORTED=NO). Tree CLEAN, HEAD unchanged; no git ops during the run. `FULL_RUN_ATTEMPT_6_STATUS=COMPLETE`; no attempt #7. **Next: GraphRAG-08 Value Evidence Architecture Review** (A — value-evidence-review-ready). | [`GRAPHRAG_08_FULL_BENCHMARK_ATTEMPT_6_RESULT.md`](GRAPHRAG_08_FULL_BENCHMARK_ATTEMPT_6_RESULT.md) |
| GraphRAG-08 — Value-evidence architecture review gate | 🟨 **OFFLINE ARCHITECTURE/DECISION REVIEW — docs only; awaiting operator review; no code, no provider traffic.** Decides whether the evidenced `/query/data` runtime seam justifies any future production-oriented GraphRAG path, given `GRAPH_RETRIEVAL_VALUE_EVIDENCED=NO` but `STRUCTURED_QUERY_DATA_RUNTIME_VALUE_EVIDENCED=YES`. **Linchpin forensic:** the committed `/query/data` request body is `{query,mode,only_need_context,top_k}` (gd_seam.py:158–164) — NO per-query Source-allowlist; `benchmark_ids` filters POST-retrieval (:188) ⇒ `PRE_RETRIEVAL_SOURCE_SCOPE_CONTROL=NO`, `POST_RETRIEVAL_FILTER_ONLY=YES` (LightRAG searches the whole workspace; ON can only discard afterward — cannot constrain graph breadth/precision/provider-work at source). Role assessment: primary-retriever / vector-replacement / RRF-input / answerability-detector / Source-expander = **NOT_SUPPORTED**; provenance-corroborator & relationship-context = CONDITIONALLY_PLAUSIBLE (value UNPROVEN); eval/research-only = SUPPORTED. `DIRECT_RRF_JUSTIFIED=NO`; `NEGATIVE_QUERY_SELF_ABSTENTION=NOT_SUPPORTED` (abstention 0.0); `MULTIHOP_INCREMENTAL_VALUE_EVIDENCED=INCONCLUSIVE` (uniform 20 cands every class); `GRAPHRAG_07_CONTRACT_STILL_VALID=YES` (contract ≠ retrieval value); `LIGHTRAG_UNIQUE_VALUE_OVER_SIMPLER_ON=NOT_PROVEN`; `ASK_INTEGRATION_JUSTIFIED_TODAY=NO`. **FINAL OPERATOR DECISION = B — KEEP_GD_AS_PROVENANCE_ONLY_RESEARCH_PATH** (`GRAPHRAG_PRODUCTION_INTEGRATION=NOT_APPROVED`, `GRAPHRAG_RESEARCH_PATH=RETAINED_OPTIONAL`). LightRAG is retained in the repo as an **OPTIONAL, feature-flagged GraphRAG research/evidence sidecar** (existing lifecycle/sidecar/indexing/`/query/data`/provenance/eval/isolation tooling remain valid research infrastructure). B does NOT approve LightRAG as primary retriever / vector replacement / Ask merge / RRF input / unconstrained Source expansion / answerability decisions / production Structured Evidence Adapter / GraphRAG-09. "Provenance-only" = the CURRENT boundary (corroborate already-selected/authorized Sources; `MECHANICALLY_FEASIBLE=PARTIAL`, `PRODUCT_VALUE=UNPROVEN`) — not a permanent ban on future expansion research. **Why B over A:** A (STOP) remains technically defensible on retrieval evidence, but the operator's explicit objective is to retain LightRAG as optional research infrastructure, and a real `/query/data` structured-runtime advantage + clean provenance were evidenced — insufficient for production today, so B preserves a narrow falsifiable research path without pretending retrieval value was shown, and preserves the ability to stop later. **Maintenance guard:** NO production dependency on LightRAG, NO eval-module import into runtime (`PRODUCTION_IMPORTS_EVAL=NO`), NO GraphRAG-required deployment/Ask path, NO mandatory sidecar for normal operation; primary production retrieval stays the ON vector path. Reopen/advance only on materially new capability/evidence (new LightRAG version with pre-retrieval Source scoping, a valid graph rank/score, a bounded provenance experiment showing incremental evidence, a concrete product need ON-native logic can't meet, or a controlled falsifiable secondary-evidence design without expansion) via a new proposal + evidence gate — never an automatic GraphRAG-09. `NEXT_EXPERIMENT_REQUIRED=NO · NEXT_EXPERIMENT_NAME=NONE · GRAPH_RAG_09_JUSTIFIED=NO`. Boundary B unchanged (synthetic/public only). C rejected (expansion unjustified + pre-retrieval scoping impossible); D rejected (evidence suffices). Retained: `RRF_CANDIDATE_INTERFACE_READY=NO`, `GRAPH_CANDIDATE_IMPLEMENTATION_READY=NO`, `QUERY_DATA_EXPOSES_VALID_RANK/SCORE=NO`, `STRUCTURED_EVIDENCE_IMPLEMENTATION_READY=NO`, `08F_MITIGATION_GATE_JUSTIFIED=NO`, `ROOT_CAUSE_CONFIRMED=NO`, H1/H2/H3 INCONCLUSIVE, `GRAPH_RAG_09_JUSTIFIED=NO`. Boundary B unchanged (not approved for internal data). Independent architecture review of the twelve §51 questions performed. No code; no migration; zero provider traffic; fixture unchanged; NOT checkpointed. | [`GRAPHRAG_08_VALUE_EVIDENCE_ARCHITECTURE_REVIEW.md`](GRAPHRAG_08_VALUE_EVIDENCE_ARCHITECTURE_REVIEW.md) |
| **GraphRAG-08 = CLOSED / APPROVED** · **GraphRAG-PN01 — Per-Notebook Isolation & QA (NEW research track, NOT GraphRAG-09)** | 🟨 **OFFLINE FORENSIC + ARCHITECTURE DESIGN ONLY — docs only; awaiting operator review; NOT checkpointed; no code, no provider traffic, no live LightRAG, no DB mutation, no migration, no Ask integration, no PN02 implementation.** A NEW product/use-case track (does NOT reopen or modify any GraphRAG-08 conclusion) triggered by a concrete requirement: each Open Notebook notebook owns its OWN isolated LightRAG workspace/index, and a question asked inside a notebook uses ONLY that notebook's graph data. **Distinct from 08's finding:** 08 proved per-QUERY Source allowlisting inside a SHARED graph is impossible (`PRE_RETRIEVAL_SOURCE_SCOPE_CONTROL=NO`); PN01 evaluates scope by WORKSPACE/INDEX OWNERSHIP — one level up — which 08 never tested. **Forensic (source-verified):** ON membership = SurrealDB `reference` RELATION edge `FROM source TO notebook`, **MANY-TO-MANY, no cardinality constraint** (`migrations/1.surrealql:54-56`) → `SOURCE_SHARED_ACROSS_NOTEBOOKS_SUPPORTED_BY_ON=YES` (ON already has first-class `shared_source_count` handling, `notebook.py:161-315`). ON Ask/Search are GLOBAL today (no notebook scoping; `ask.py:106`, `fn::vector_search` `1.surrealql:139-173`); only Chat is notebook-scoped, via full-context injection (`notebook.py:70-134`) — so notebook-scoped RETRIEVAL is trivially ON-native (a `reference`-join filter). **LightRAG v1.5.6 `workspace` = STRONG hard partition** (separate on-disk subdir + in-memory namespace key; second-hand v1.5.6 forensic `cell_isolation08.py:12-24`) **but FIXED at process startup** (`WORKSPACE` env / `--workspace`; NO per-query/header/body selector; `cell_isolation08.py:21-24`, `cell_provisioner08.py:559`) → per-notebook isolation needs one sidecar PROCESS/CONTAINER per active workspace; production ON sets NO workspace (single default, isolation UNEXERCISED; `client.py`/`gd_seam.py` carry no workspace). **Decisions:** `PER_NOTEBOOK_WORKSPACE_TECHNICALLY_FEASIBLE=YES`, `PER_NOTEBOOK_QUERY_ROUTING_FEASIBLE=PARTIAL` (process-selection, not per-query param), `PER_NOTEBOOK_INDEX_LIFECYCLE_FEASIBLE=PARTIAL` (shared-source fan-out + per-workspace 03-series generalization), `CROSS_NOTEBOOK_GRAPH_ISOLATION_FEASIBLE=YES` (structural, still needs ON post-validation backstop), `WORKSPACE_DATA_ISOLATION=STRONG`, `DUPLICATE_PER_NOTEBOOK_INDEXING_ACCEPTABLE=CONDITIONAL`, `PREFERRED_GRAPH_QUERY_SEAM=/query/data`, `PREFERRED_FINAL_ANSWER_OWNER=OPEN_NOTEBOOK`, `LIGHTRAG_REQUIRED_FOR_NORMAL_NOTEBOOK_QA=NO`. **Value:** isolation gives real pre-retrieval NOTEBOOK-level scoping (new vs 08) but only SHRINKS the corpus — it does NOT fix 08's intra-graph precision/abstention/no-rank defects (`PER_NOTEBOOK_RETRIEVAL_VALUE=UNKNOWN`); notebook scoping is ON-native-trivial so `LIGHTRAG_PER_NOTEBOOK_UNIQUE_VALUE=NOT_PROVEN`; `PER_NOTEBOOK_LIGHTRAG_VALUE_EVIDENCED=NOT_YET`. **`PN01_DECISION=C — PER_NOTEBOOK_ARCHITECTURE_FEASIBLE_AND_BOUNDED_EVALUATION_JUSTIFIED`** (narrow, research-only; B "not worth evaluating" co-defensible; A/D rejected). **`PN02_EVALUATION_JUSTIFIED=YES` (design only)** — GraphRAG-PN02 synthetic per-notebook isolation & QA eval (primary metric `CROSS_NOTEBOOK_LEAKAGE_RATE=0`; must NOT rerun 08's 75-source global corpus; `PN02_REQUIRES_NEW_CODE=YES`). Independent review of the twelve §45 questions: no decision-flipping HIGH (question I — isolation ≠ retrieval-quality fix — folded into narrow-C). Retained frozen: `GRAPHRAG_PRODUCTION_INTEGRATION=NOT_APPROVED`, `LIGHTRAG_ASK_INTEGRATION=NOT_APPROVED`, `RRF_CANDIDATE_INTERFACE_READY=NO`, `GRAPH_CANDIDATE_IMPLEMENTATION_READY=NO`, `STRUCTURED_EVIDENCE_IMPLEMENTATION_READY=NO`, **`GRAPH_RAG_09_JUSTIFIED=NO`**. Boundary B unchanged (synthetic/public only). No code; no migration (count 50); zero provider traffic; sidecar not started; `GRAPHRAG_ENABLED` untouched; fixture unchanged; NOT checkpointed. | [`GRAPHRAG_PN01_PER_NOTEBOOK_ISOLATION_QA_FORENSIC.md`](GRAPHRAG_PN01_PER_NOTEBOOK_ISOLATION_QA_FORENSIC.md) |
| **GraphRAG-PN02A — Synthetic Per-Notebook Isolation & QA Evaluation (DESIGN FREEZE)** | 🟨 **OFFLINE EXPERIMENT DESIGN ONLY — docs only; awaiting operator review; NOT checkpointed; no code, no provider traffic, no live LightRAG, no DB mutation, no PN02B implementation, no Ask integration, no RRF, no GraphRAG-09.** Freezes a small falsifiable synthetic benchmark answering TWO separated questions: **(Q-ISO)** does per-notebook workspace isolation give safe notebook-scoped graph evidence (leakage=0)? and **(Q-VAL)** does notebook-local GD evidence add useful QA value in SMALL (~8-Source) notebooks, or does 08's broad-candidate behavior persist at notebook scale? Explicitly NOT a rerun of 08's 75-Source global corpus. **Two-stage gate:** Stage 1 (isolation + evidence validity, HARD fail-closed gates) must PASS before Stage 2 (ON-owned QA value). **Frozen fixture `graphrag_pn02_eval_v1`** (new; `graphrag_08_eval_v1` + hash untouched): **3 notebooks**, **21 canonical Sources** (18 unique A1-6/B1-6/C1-6 + 3 shared SH_AB/SH_AC/SH_BC), **24 membership edges**, **8 Sources/notebook**; deliberate cross-notebook lexical/entity collisions (e.g. "Project Orion" code AX-17 in A / BX-94 in B / CX-51 in C) as leakage bait. **8 query classes × 3 notebooks = 24 queries** (DIRECT_LOCAL, SEMANTIC_LOCAL, MULTIHOP_LOCAL, SHARED_SOURCE, CROSS_NOTEBOOK_COLLISION, NEGATIVE_UNANSWERABLE, RELATIONSHIP_CORROBORATION, PARTIAL_EVIDENCE); manual pre-retrieval GT with explicit REQUIRED/OPTIONAL/**FORBIDDEN**/expected+forbidden answer facts/abstention/multihop. **Systems:** V = notebook-scoped ON vector baseline (eval-only `WHERE source IN member_ids` seam via `reference` edge — structurally leak-proof; production `fn::vector_search` untouched), GD = notebook-isolated `/query/data` (unordered, no rank/score, top_k unset to observe breadth); **GQ_REQUIRED=NO** (08 GQ≡GD parity). VECTOR_K={3,5} (QA uses K=5). **HARD Stage-1 gates:** CROSS_NOTEBOOK_LEAKAGE_RATE / leak_query_count / leak_source_occurrences / provenance_foreign / provenance_malformed / citation_membership_invalid ALL = 0 (no averaging away a leak). Incremental-value metrics vs V(K): GRAPH_NEW_REQUIRED_SOURCES / GRAPH_NEW_FALSE_POSITIVES / incremental precision / multihop incremental recovery; **NO RRF** (union diagnostic only). **Stage 2:** FINAL_ANSWER_OWNER=OPEN_NOTEBOOK; arms QA-V / QA-GD / QA-V+GD (frozen deterministic combination — GD member-Sources appended by canonical ID, NO fake rank, cap 8, non-members dropped); deterministic answer-token + citation grading authoritative (no LLM-as-truth); CROSS_NOTEBOOK_ANSWER_LEAKAGE_RATE=0 required. **Membership-removal test INCLUDED** (SH_AB removed from A, kept in B → must vanish as A evidence, remain B; stale-graph-evidence-accepted=0 defense-in-depth); source-update & notebook-delete tests EXCLUDED (deferred, lifecycle not value). Workspace topology = one container/workspace per notebook, attested before query (owned container/workspace/storage/binding/v1.5.6/endpoint/mapping); opaque routing `nb_+sha256(notebook_id)[:16]`. **Bounded workload:** ≤21 canonical embeddings, ≤24 graph-index ops (≤48 attempts ×2), ≤26 GD queries, ≤72 final-answer calls; retries index/source=2, query=1, final-answer=0 (no retry-to-improve). Fixture hash required, frozen at PN02B. Decision rules **refined to fully mechanical** first-match tables (no vague adjectives): retrieval-value §17b (R1 NEW_REQ==0→NO; R2 all-gain-from-full-8-return→NO per 08 §40; R3 N_GAIN_NOTEBOOKS_D≥2 ∧ NEW_REQ>NEW_FP ∧ GD-negatives≤V5-negatives→YES; else INCONCLUSIVE), QA-value §17c (YES iff ∃ arm SAFE[S1/S2/S3=0] ∧ strict-improve ≥1 of P1/P2/P3 ∧ no-regress), multihop §17d (MH≥2→YES/==0→NO/==1→INCONCLUSIVE); exact §9c metric formulas; the "one-required-gain + many-false-positives ⇒ YES" loophole closed by NEW_REQ>NEW_FP; forbidden-fact hallucination is a hard S3=0 safety gate. Workload fully itemized (§13): 21 canonical embeddings, 24 graph-index ops (≤48 attempts), 26 GD (24+2 removal), 26 vector queries/embeddings, 72 final-answer (24×3), judge=0; membership-removal fits inside caps (0 reindex, 0 final-answer). Independent review of the sixteen §58 questions + a focused refinement review of the ten §16 mechanical/workload questions: no HIGH, no unresolved MEDIUM. **`PN02A_DECISION=C — PN02_DESIGN_FROZEN_AND_IMPLEMENTATION_JUSTIFIED`**; `PN02_IMPLEMENTATION_JUSTIFIED=YES`, **`PN02_LIVE_AUTHORIZED=NO`**; next = **GraphRAG-PN02B — harness implementation (OFFLINE only, NOT_STARTED)**. Retained frozen: `GRAPHRAG_PRODUCTION_INTEGRATION=NOT_APPROVED`, `LIGHTRAG_ASK_INTEGRATION=NOT_APPROVED`, `RRF_CANDIDATE_INTERFACE_READY=NO`, **`GRAPH_RAG_09_JUSTIFIED=NO`**. Boundary B unchanged (synthetic/public only). No code; no migration (count 50); zero provider traffic; sidecar not started; `GRAPHRAG_ENABLED` untouched; fixtures unchanged; NOT checkpointed. | [`GRAPHRAG_PN02A_SYNTHETIC_PER_NOTEBOOK_QA_EVAL_DESIGN.md`](GRAPHRAG_PN02A_SYNTHETIC_PER_NOTEBOOK_QA_EVAL_DESIGN.md) |
| GraphRAG-PN02C — Synthetic per-notebook live preflight, workspace provisioning & routing attestation gate | ✅ **ROUTING / ATTESTATION PREFLIGHT APPROVED — checkpointed at tag `graphrag-pn02c-routing-preflight-approved`; NOT the live benchmark; no indexing, no embedding, no LLM, no `/query/data`, no `client.query`, no document insert, no final answer, no normal-DB mutation, no real internal data, no Ask/GraphRAG production integration, no RRF, no GraphRAG-09.** Proves the approved per-notebook topology (NB_A/B/C → runtime/workspace/storage/endpoint A/B/C) can be provisioned and attested **with ZERO provider traffic** before any provider-backed PN02 live run (baseline HEAD `8eff128`, tag `graphrag-pn02b-offline-harness-approved`, tree CLEAN). **Fixture `graphrag_pn02_eval_v1` revalidated** — `FIXTURE_VALIDATION=PASS`, `FIXTURE_HASH_VALIDATION=PASS` (`9ce7df74…6899a6`, unchanged); 3 nb / 21 src / 24 memberships / 24 queries; workspace ids `nb_2801282a…`/`nb_ca49abb9…`/`nb_45f0f09a…` (opaque, from `notebook:gr_pn02_{a,b,c}` record ids, never titles). **Runtime model (design decision, disclosed):** the real LightRAG runtime is a Docker **v1.5.6** sidecar (not a pip package) whose startup provider-safety is not guaranteed (08E history); booting it risks provider traffic for zero preflight value, so PN02C instead provisions **real owned local resources** — 3 distinct loopback endpoints (real `127.0.0.1:<port>` bound by owned stdlib **attestation micro-runtimes** serving ONLY `/health`+`/identity`, 404 for `/query/data`+`/documents`, no provider code path), 3 distinct run-owned storage roots (`mkdtemp`, forbidden-root guard), 3 distinct owned **attestation micro-runtimes — NOT real LightRAG runtimes** (`ATTESTATION_RUNTIME_COUNT=3`, `REAL_LIGHTRAG_RUNTIME_COUNT=0`, `REAL_LIGHTRAG_RUNTIME_BOOTED=NO`, `REAL_LIGHTRAG_RUNTIME_BOOT_PROVEN_PROVIDER_FREE=NO`, `REAL_LIGHTRAG_PROVIDER_FREE_BOOT_PROVEN=NO`; A→B→C startup, per-step alive/endpoint/storage/config-version/provider-traffic verification, partial-startup→full rollback). LightRAG version = **config-pin match, NOT a runtime attestation**: `LIGHTRAG_CONFIG_VERSION_PIN_MATCH=PASS` (via `VERIFIED_LIGHTRAG_VERSION` `v1.5.6`), `REAL_LIGHTRAG_RUNTIME_VERSION_ATTESTED=NO`, `REAL_LIGHTRAG_RUNTIME_VERSION=NOT_PROBED` — the real Docker sidecar was NOT booted (a safety decision per 08E history, not a failure); real-runtime boot + version probe DEFERRED to PN02D. **PN02C did NOT prove:** real LightRAG process startup / workspace loading / runtime version / storage-backend init / provider-free startup / `/query/data` / post-index graph isolation. **3 new eval-only modules** under `integrations/graphrag/eval/` (`provisionpn02c`/`routingpn02c`/`preflightpn02c` — never imported by production): real resource provisioning + collision checks; real-endpoint attestation (reuses PN02B `WorkspaceAttestation`/`require_attested_before_query`/`provider_binding_fingerprint`) + fail-closed router; orchestrator + provider-traffic guard + content-safe manifest/attestation/routing/workload reports + eval-only preflight CLI. **End-to-end preflight EXECUTED (`pn02c-live-1`, real loopback attestation micro-runtimes): outcome COMPLETED, decision C.** `WORKSPACE_A/B/C_ATTESTED=PASS`, `ALL_WORKSPACES_ATTESTED=PASS`; `WRONG_ENDPOINT_ROUTING_REJECTED=PASS`, `WRONG_WORKSPACE_ROUTING_REJECTED=PASS`, `UNATTESTED_QUERY_AUTHORIZATION_REJECTED=PASS`, `CROSS_WORKSPACE_STORAGE_ALIASING=0`; `ENDPOINT_COLLISION_CHECK=PASS`, `STORAGE_COLLISION_CHECK=PASS`; `SHARED_SOURCE_WORKSPACE_PLAN=PASS` (SH_AB→{A,B}, SH_AC→{A,C}, SH_BC→{B,C}), provisioning plan 8+8+8=24, removal plan (delete=1, post-removal GD=2/vector=2/final=0); `WORKLOAD_LEDGER_PREFLIGHT=PASS` (caps validated, NOT consumed). **All actual provider workload consumed = 0** (graph-index 0 / attempts 0 / GD 0 / vector 0 / embedding 0 / final-answer 0 / judge 0 / `/query/data` 0 / `client.query` 0 / doc-insert 0 / external-provider-network 0 / normal-DB mutations 0). `PROVIDER_BINDING_CONFIGURED` reported by env var NAME only (never value); provider model config attested by name (llm `openai/gpt-4o-mini`, embed `openai/text-embedding-3-small`, dim 1536) without calls; secret-scan of every artifact clean. **Cleanup:** `PN02C_OWNED_PROCESSES_REMAINING=0`, `PN02C_RUNTIME_STORAGE_RESIDUE=0`; reproducibility run twice clean; normal ON/SurrealDB/other processes untouched. **Tests: 28 PN02C pass** (3 distinct workspace/endpoint/storage, wrong-route + unattested + version-mismatch + fixture-hash-mismatch rejection, duplicate endpoint/storage detection, provider-workload non-consumption, cleanup semantics + in-flight-failure rollback, manifest secret-safety, provisioning/shared/removal plans, full happy-path + reproducibility, no-provider-seam source guard); GraphRAG regression (flag off) **832 pass / 9 skip / 0 fail**; ruff + mypy clean on all 3 modules. **`PN02C_DECISION=C — PN02C_PREFLIGHT_PASSED_AND_LIVE_AUTHORIZATION_GATE_JUSTIFIED`** — C means the routing/attestation/preflight machinery is validated enough to justify a SEPARATE operator-controlled live authorization gate; **C does NOT mean** real LightRAG runtime attested / provider-backed execution authorized / fixture indexing authorized / `/query/data` authorized / QA authorized (C authorizes NO traffic). The separate gate (e.g. GraphRAG-PN02D) is NOT started automatically and its mandatory FIRST step is **`REAL_LIGHTRAG_BOOT_GATE`** (`REAL_LIGHTRAG_BOOT_GATE_REQUIRED_BEFORE_INDEX=YES`): start ONE run-owned real LightRAG v1.5.6 sidecar → verify actual running version → verify workspace/storage identity → verify `EXTERNAL_PROVIDER_NETWORK_CALLS=0` on startup → shutdown/cleanup; only then (and after a separate 3-runtime real-LightRAG attestation) may indexing be considered — if startup causes any provider traffic, STOP before indexing. `PN02C=ROUTING / ATTESTATION PREFLIGHT APPROVED`, `REAL_LIGHTRAG_BOOT=NOT_YET_ATTESTED`, `PN02_LIVE=NOT_AUTHORIZED` (real LightRAG runtime validation is NOT marked complete). `PER_NOTEBOOK_ISOLATION_EVIDENCED=NOT_YET`, `..._GRAPH_RETRIEVAL_VALUE=NOT_YET`, `..._GRAPH_QA_VALUE=NOT_YET`, `..._MULTIHOP_INCREMENTAL_VALUE=NOT_YET` (infrastructure attestation ≠ retrieval-isolation evidence under indexed data). **`PN02_LIVE_AUTHORIZED=NO`.** Retained frozen: `GRAPHRAG_PRODUCTION_INTEGRATION=NOT_APPROVED`, `LIGHTRAG_ASK_INTEGRATION=NOT_APPROVED`, `RRF_CANDIDATE_INTERFACE_READY=NO`, `STRUCTURED_EVIDENCE_IMPLEMENTATION_READY=NO`, **`GRAPH_RAG_09_JUSTIFIED=NO`**, `PRODUCTION_IMPORTS_EVAL=NO`. Boundary B unchanged (`REAL_INTERNAL_DATA_ALLOWED=false`, `SYNTHETIC_ONLY=true`). No provider traffic; real LightRAG sidecar not started; `GRAPHRAG_ENABLED` untouched; no `.env`; no migration (count 50); fixture unchanged; checkpointed at tag `graphrag-pn02c-routing-preflight-approved`. | [`GRAPHRAG_PN02C_LIVE_PREFLIGHT_ROUTING_ATTESTATION.md`](GRAPHRAG_PN02C_LIVE_PREFLIGHT_ROUTING_ATTESTATION.md) |
| GraphRAG-PN02D-A — Real LightRAG boot & data-plane attestation gate | ✅ **REAL LIGHTRAG DATA-PLANE PREFLIGHT APPROVED — checkpointed at tag `graphrag-pn02da-real-lightrag-preflight-approved`; Gate 0 PASS + Gate 1 PASS; NOT the live benchmark; no indexing, no embedding, no LLM, no `/query/data`, no `client.query`, no document insert, no final answer, no judge, no normal-DB mutation, no real internal data, no Ask/GraphRAG production integration, no RRF, no GraphRAG-09.** Executes the mandatory PN02C `REAL_LIGHTRAG_BOOT_GATE`: replaces PN02C's attestation micro-runtimes with the **REAL `ghcr.io/hkuds/lightrag:v1.5.6` Docker engine** and proves — BEFORE ANY INDEXING — that the per-notebook topology boots and attests with ZERO provider traffic and ZERO data (baseline HEAD `7553c0f`, tag `graphrag-pn02c-routing-preflight-approved` peel `7553c0f`, tree CLEAN). **Fixture `graphrag_pn02_eval_v1` revalidated** — `FIXTURE_VALIDATION=PASS`, `FIXTURE_HASH_VALIDATION=PASS` (`9ce7df74…6899a6`, unchanged); no Source indexed. **Provider-egress guard = 3 independent layers:** (1) kernel — each sidecar on its OWN run-owned `--internal` Docker network (`Internal=true`, no route off-host → egress impossible by construction); (2) config — EMPTY provider bindings + no `LIGHTRAG_API_KEY` (no secret, nothing to call; `assert_no_provider_binding_in_command` refuses a nonempty provider target); (3) detection — socket-level parse of the container's `/proc/net/tcp{,6}` via `docker exec`, counting any non-loopback remote peer (fail-closed `ProviderEgressGuard`). Image defines NO HEALTHCHECK → nil-safe inspect template + HTTP `/health` status determines health. **Gate 0 (single real sidecar `pn02da-live-1-g0`): PASS** — booted healthy ~7s; `REAL_LIGHTRAG_RUNTIME_BOOTED=YES`, `REAL_LIGHTRAG_RUNTIME_VERSION_ATTESTED=YES`, `REAL_LIGHTRAG_RUNTIME_VERSION=v1.5.6` from **three provider-free runtime signals** (health `core_version=1.5.6` · `lightrag.__version__=1.5.6` · image label `v1.5.6`); workspace `nb_9af1e7f8…` (observed==expected), storage `st_089d2b7c…` (run-owned, forbidden-root-guarded, fresh), endpoint owned; `EXTERNAL_PROVIDER_NETWORK_CALLS=0`, doc-insert/`/query/data`/`client.query`/embedding/LLM = 0; cleanup 0/0/0. **Gate 1 (three real runtimes A/B/C `pn02da-live-1-g1`, only after Gate 0 PASS): PASS** — `REAL_LIGHTRAG_RUNTIME_COUNT=3`, `WORKSPACE_COUNT=3`, `STORAGE_ROOT_COUNT=3`, `ENDPOINT_COUNT=3` (one-to-one; workspaces `nb_2801282a…`/`nb_ca49abb9…`/`nb_45f0f09a…` match PN02C exactly); `REAL_LIGHTRAG_ALL_VERSION_ATTESTED=PASS`, `REAL_LIGHTRAG_ALL_WORKSPACES_ATTESTED=PASS`, `REAL_LIGHTRAG_STORAGE_ALIASING=0`; routing negatives (reuse PN02C contract vs REAL endpoints, NO `/query/data`) `REAL_WRONG_ENDPOINT_ROUTE_REJECTED`/`REAL_WRONG_WORKSPACE_ROUTE_REJECTED`/`REAL_UNATTESTED_ROUTE_REJECTED`/`CORRECT_ROUTE_ACCEPTED` all PASS; `REAL_LIGHTRAG_THREE_RUNTIME_PROVIDER_TRAFFIC=0`; shared-source plan SH_AB→{A,B}/SH_AC→{A,C}/SH_BC→{B,C} reverified (not indexed); cleanup 0/0/0. **HARD no-index capability:** `RealLightRAGPreflightAuthorization` (mirrors PN02B `Stage1Authorization`, unforgeable) minted ONLY on Gate 0 PASS ∧ Gate 1 PASS; `require_real_preflight_authorization` gates any future indexing seam — PN02D-A implements NO indexing. **4 new eval-only modules** under `integrations/graphrag/eval/` (`egressguardpn02d`/`realsidecarpn02d`/`attestpn02d`/`preflightpn02d` — never imported by production): socket egress guard; real-sidecar boot command/config + injectable `DockerCLI` (targeted content-safe inspects/execs only) + nil-safe inspect + `RealTopology`/cleanup; 3-signal version attestation + full runtime attestation + authorization capability; Gate 0+Gate 1 orchestrator + decision A/B/C + content-safe report + eval-only `--live` CLI. **Provider binding status (future config, name-only, presence≠authorization):** `LLM_PROVIDER_CONFIG_PRESENT=NO`, `EMBEDDING_PROVIDER_CONFIG_PRESENT=NO`, `OPENROUTER_KEY_PRESENT=NOT_REQUIRED_FOR_BOOT`, `SIDE_CAR_AUTH_CONFIG_PRESENT=NO`. **Cleanup/normal-service safety:** Docker container+network listings IDENTICAL before/after (only unrelated `open-notebook-surrealdb-1` + standard networks; untouched); `REAL_LIGHTRAG_OWNED_PROCESSES_REMAINING=0`, `REAL_LIGHTRAG_RUNTIME_STORAGE_RESIDUE=0`, owned networks 0; `NORMAL_DB_MUTATIONS=0`; run twice clean; no secret generated/printed/persisted. **Tests: 34 PN02D-A offline pass** (command/config gen, version/workspace/storage mismatch reject, provider-attempt reject, 3-runtime identity/storage/endpoint uniqueness, wrong-route reject, cleanup ownership, fixture-hash mismatch reject, no-index-before-both-gates, unforgeable authorization, egress parsing, no-provider-seam guard) via injectable `FakeDocker`, plus the one authorized provider-free real-Docker integration executed as this gate; GraphRAG+PN02 regression **866 pass / 9 skip / 0 fail**; ruff + mypy clean on all 4 modules. **`PN02DA_DECISION = C — REAL_LIGHTRAG_PREFLIGHT_PASSED_AND_PROVIDER_RUN_AUTHORIZATION_GATE_JUSTIFIED`** — C means the real data-plane topology boots/attests safely with zero traffic so a SEPARATE operator-controlled provider-run authorization gate is justified; **C authorizes NO provider run.** `REAL_LIGHTRAG_BOOT_GATE=PASS`, `PN02_PROVIDER_RUN_AUTHORIZATION_GATE_JUSTIFIED=YES`, **`PN02_PROVIDER_RUN=NOT_AUTHORIZED`** (`PN02_PROVIDER_RUN_AUTHORIZED=NO`). `PER_NOTEBOOK_ISOLATION_EVIDENCED=NOT_YET`, `..._GRAPH_RETRIEVAL_VALUE=NOT_YET`, `..._GRAPH_QA_VALUE=NOT_YET`, `..._MULTIHOP_INCREMENTAL_VALUE=NOT_YET` (a real empty-workspace boot is infrastructure evidence, NOT indexed-data isolation evidence). Retained frozen: **GraphRAG-08 = CLOSED / APPROVED**, PN01/PN02A/PN02B/PN02C = APPROVED, `GRAPHRAG_PRODUCTION_INTEGRATION=NOT_APPROVED`, `LIGHTRAG_ASK_INTEGRATION=NOT_APPROVED`, **`GRAPH_RAG_09_JUSTIFIED=NO`**, `PRODUCTION_IMPORTS_EVAL=NO`. Boundary B unchanged (`REAL_INTERNAL_DATA_ALLOWED=false`, `SYNTHETIC_ONLY=true`). No `.env`; no migration (count 50); `GRAPHRAG_ENABLED` untouched; fixture unchanged; live benchmark NOT started; checkpointed at tag `graphrag-pn02da-real-lightrag-preflight-approved`. | [`GRAPHRAG_PN02DA_REAL_LIGHTRAG_DATA_PLANE_PREFLIGHT.md`](GRAPHRAG_PN02DA_REAL_LIGHTRAG_DATA_PLANE_PREFLIGHT.md) · `tests/test_graphrag_pn02d_preflight.py` |
| GraphRAG-PN02D-B1 — Synthetic indexed isolation & GD evidence (first provider-backed run) | ⛔ **BLOCKED — pre-provider; ZERO provider traffic occurred and none was possible; NOT checkpointed (operator review required).** `RUN_ID=pn02db1-blocked-ee59ad02b2b1`. Baseline verified from git (branch `feature/graphrag-lifecycle`, HEAD `8e09617`, tag `graphrag-pn02da-real-lightrag-preflight-approved` peel `8e09617`, tree CLEAN). Fixture `graphrag_pn02_eval_v1` revalidated `FIXTURE_VALIDATION=PASS`/`FIXTURE_HASH_VALIDATION=PASS` (`9ce7df74…6899a6`). Raw preconditions present (LightRAG `v1.5.6` image cached, `OPENROUTER_API_KEY` in `.env`, Docker up) but **the committed harness has NO live provider-backed PN02 driver**: per-notebook indexer / GD-`/query/data` / vector-baseline seams exist ONLY as inert refusing stubs (`eval/live_seam_pn02.py`, `PN02_LIVE_AUTHORIZED=False`, "no hidden HTTP client"); `offlinepn02.py` has no `run-benchmark` verb (never touches provider/LightRAG/socket); PN02D-A `attestpn02d` "does NOT index" and only mints a capability for a **future** indexing seam that has not been built; the only live runners (`runner.py`/`runner08.py`) are GraphRAG-04/08 single-workspace and use the forbidden `client.query`/final-answer path. Runtime-proven refusal: `index_membership`/`vector.query` → `LiveExecutionNotAuthorized`. Consequently `INDEXED_WORKSPACE_MEMBERSHIPS=0/24 → FAILED_BEFORE_QUERY`; Stage-1, isolation, retrieval R0–R4, multi-hop M0–M3, membership-removal all `NOT_EVALUATED`; actual workload 0 across every category (`NORMAL_DB_MUTATIONS=0`, `OWNED_PROCESSES_REMAINING=0`, `RUNTIME_STORAGE_RESIDUE=0`). **`B1_DECISION=A — PN02D_B1_BLOCKED_LIVE_DRIVER_ABSENT`**; `PN02D_B2_QA_AUTHORIZATION_GATE_JUSTIFIED=NO` (decision C impossible — hard technical validity blocker); `PER_NOTEBOOK_GRAPH_QA_VALUE_EVIDENCED=NOT_YET`; `PN02_PROVIDER_RUN_AUTHORIZED=NO` retained. Driver **not** built this turn (out of scope per AGRIBANK §2.5/§12; it is ~5 provider/security-sensitive subsystems needing their own design→offline-impl→review→approval phase, as 08E.4/.5/.7B were). Targeted tests **53 pass** (pn02d preflight + pn02 offline + stage1); no implementation code changed (docs/artifacts only). **Recommended next step:** authorize a separate offline live-wiring phase (proposed **PN02D-B0**) to build the driver with zero traffic, review/approve, then re-authorize B1. Retained frozen: GraphRAG-08 CLOSED/APPROVED, `GRAPH_RAG_09_JUSTIFIED=NO`, `GRAPHRAG_PRODUCTION_INTEGRATION=NOT_APPROVED`, `LIGHTRAG_ASK_INTEGRATION=NOT_APPROVED`, Boundary B (`SYNTHETIC_ONLY=true`). | [`GRAPHRAG_PN02DB1_INDEXED_ISOLATION_GD_EVIDENCE.md`](GRAPHRAG_PN02DB1_INDEXED_ISOLATION_GD_EVIDENCE.md) · `.artifacts/graphrag-pn02db1/ee59ad02b2b1/manifest.json` |
| GraphRAG-PN02D-B0A — Per-notebook provider live-driver design gate | ✅ **DESIGN / FORENSIC COMPLETE + REFINED (B0A-R1) — APPROVED; checkpointed at tag `graphrag-pn02db0a-live-driver-design-approved` (commit `a2e8303`); DESIGN ONLY — authorizes no B0B implementation, no provider traffic, no real LightRAG boot, no indexing, no embeddings, no `/query/data`, no vector live query, no B1 re-run, no B2, no production Ask/GraphRAG, no GraphRAG-09; NO code, ZERO provider traffic, no real LightRAG boot, no indexing, no embeddings, no `/query/data`, no `client.query`, no vector calls, no final answers, no Ask integration, no RRF, no GraphRAG-09.** Designs (only) the missing provider-backed PN02 driver whose absence fail-closed-blocked PN02D-B1 (`B1_BLOCKER=MISSING_APPROVED_LIVE_DRIVER`). Baseline verified from git (branch `feature/graphrag-lifecycle`, HEAD `162b3d1`, tag `graphrag-pn02db1-live-driver-blocked` peel `162b3d1`, tree CLEAN). **Source-verified seam forensic** across PN02B evaluator + PN02C/PN02D-A live topology + historical GraphRAG-04/08 primitives + the ON vector seam. **Key findings:** the static foundation already exists and is reusable — unforgeable `RealLightRAGPreflightAuthorization` (`attestpn02d.py:188-245`) + `Stage1Authorization` (`stage1pn02.py`) capability pattern; `routing_manifest`/`route_query`/`authorize_future_gd` + `RealTopology` (1 notebook→1 runtime/workspace/storage/endpoint); frozen provider binding (`provider_binding08.py`, openai/gpt-4o-mini + text-embedding-3-small dim 1536, secret by env-inheritance); GD `/query/data` client (`gd_seam.py`, `only_need_context=true`, post-filter only, no `client.query`/final-answer); index submit/poll + frozen retry twin (`live_indexer08.py`); PN02B result schemas + `check_cap` + `evaluatepn02` decision engine. **Net-new eval-only gaps identified:** notebook-scoped vector executor (ON `fn::vector_search` is GLOBAL, no pre-filter, `9.surrealql`; membership = `reference` edge — net-new scoped query), provider-bound PN02 sidecar boot (PN02D-A boots provider-FREE by design), a new operator-granted `PN02ProviderRunAuthorization` capability, a stateful budget guard wrapping `check_cap`, and the 3-notebook orchestrator. **Why existing runners can't be reused:** single global sidecar/base_url + single default workspace + global corpus + wrong (04/08) result schema + no notebook routing + `doc_id="doc-"+md5(source_id)` is workspace-agnostic (per-notebook delete isolation depends on routing DELETE to the notebook's own endpoint). Frozen: 4-stage fail-closed authorization chain (RealPreflight→ProviderRun→Indexing→Query), operation allowlist (structural reject of `client.query`/final-answer/QA/judge), one-notebook-one-runtime routing invariant (validate-before-send), canonical Source vs workspace membership vs derived graph document terminology, shared-source dual-instance indexing, 24/24 index-completeness hard gate, membership-removal ordering + endpoint-scoped delete + `post_validate` stale-evidence backstop (`STALE_…=0` even on delete failure), workload caps (24/48/1/26/26/26; **B1 final-answer cap=0** distinct from the ledger's Stage-2 72), conservative separate concurrency (index/GD/vector=1 initially), secret-safe content-free artifacts, technical-vs-scientific error taxonomy (mapped to `schemaspn02.TechnicalOutcome`), owned-only `try/finally` cleanup, proposed 10-module eval-only layout, 4 separate CLI verbs (`execute-b1` impossible without the capability), provider-free dry-run, and a falsifiable 21-item B0B offline test plan with a `FakeProvider/FakeLightRAG/FakeVector` backend contract. **No duplicate scientific logic** — driver emits records → `evaluatepn02` owns all R0-R4/Q0-Q3/M0-M3/Stage-1/grading. **Refinement B0A-R1 (pre-checkpoint, docs only)** resolved three load-bearing ambiguities: (1) **identity** — three separate ids (`CANONICAL_SOURCE_ID` / `WORKSPACE_ID` / `DERIVED_LIGHTRAG_DOCUMENT_ID`), authoritative logical key `(workspace_id, canonical_source_id)`, `ENDPOINT_IDENTITY_IS_DOCUMENT_IDENTITY=NO`, and **verified** vendor semantics `doc_id="doc-"+md5(file_source=source_id)` **content-independent** (`client.py:64-80,268`) → delete uses workspace+document, `SOURCE_UPDATE_TEST_INCLUDED=NO`; (2) **vector** — `PN02_VECTOR_BASELINE_CANDIDATE_UNIVERSE=CURRENT_NOTEBOOK_MEMBERS_ONLY`, `GLOBAL_TOPK_THEN_POSTFILTER_ALLOWED=NO`, frozen exact approach A (member-ID pre-ranking filter over `source_embedding`; B = exact equivalent), K3/K5 one ranking, removal excludes SH_AB from A's candidate universe before ranking; (3) **ordering** — RealPreflight→ProviderRunAuthorization→binding materialization→op authorization→provider op (`PROVIDER_AUTHORIZATION_PRECEDES_BINDING=YES`, capability required not a boolean, `B0B_PROVIDER_TRAFFIC=0`). Refinement review §21 A-I: **no HIGH / no MEDIUM / no LOW**. Independent review of the fifteen §49 questions (A-O): **no HIGH / no MEDIUM / no LOW** unresolved design findings. **`B0A_DECISION = C — LIVE_DRIVER_DESIGN_FROZEN_AND_OFFLINE_IMPLEMENTATION_JUSTIFIED`**; `PN02D_B0B_IMPLEMENTATION_JUSTIFIED=YES`, `PN02_PROVIDER_RUN_AUTHORIZED=NO`, `PN02D_B1_REAUTHORIZATION=NOT_YET`. Next = **GraphRAG-PN02D-B0B — Offline Per-Notebook Live Driver Implementation** (ZERO provider traffic). Retained frozen: GraphRAG-08 CLOSED/APPROVED, GraphRAG-09 NOT_JUSTIFIED, `GRAPHRAG_PRODUCTION_INTEGRATION=NOT_APPROVED`, `LIGHTRAG_ASK_INTEGRATION=NOT_APPROVED`, `PRODUCTION_IMPORTS_EVAL=NO`, Boundary B (`SYNTHETIC_ONLY=true`). No code; no migration (count 50); zero provider traffic; sidecar not started; `GRAPHRAG_ENABLED` untouched; no `.env`; fixture unchanged; NOT checkpointed. | [`GRAPHRAG_PN02DB0A_PER_NOTEBOOK_LIVE_DRIVER_DESIGN.md`](GRAPHRAG_PN02DB0A_PER_NOTEBOOK_LIVE_DRIVER_DESIGN.md) |
| GraphRAG-PN02D-B0B — Offline per-notebook live-driver implementation | ✅ **OFFLINE IMPLEMENTATION APPROVED — checkpointed at tag `graphrag-pn02db0b-offline-live-driver-approved` (commit `fb21bc1`); ZERO provider traffic, no real LightRAG boot, no indexing, no embedding, no `/query/data`, no `client.query`, no live vector query, no final answers, no B1 execution, no B2, no production Ask/GraphRAG, no RRF, no GraphRAG-09.** Implements the frozen B0A live-driver architecture as eval-only code (baseline HEAD `6a4cf89`; design freeze `a2e8303`, tag `graphrag-pn02db0a-live-driver-design-approved` peel `a2e8303`; tree CLEAN; fixture `graphrag_pn02_eval_v1` hash `9ce7df74…6899a6` verified live, unmodified). **13 new eval-only modules** under `integrations/graphrag/eval/` (never imported by production, `PRODUCTION_IMPORTS_EVAL=NO` verified by a repo scan test): `docidpn02d` (derived doc id `doc-+md5(source_id)`, `(workspace_id, canonical_source_id)` key, run-owned mapping store, endpoint≠identity), `budgetlivepn02d` (stateful pre-op guard wrapping `workloadpn02.check_cap`; B1 final-answer/client-query/judge caps=0), `authlivepn02d` (`ProviderOperationClass` allowlist + unforgeable `PN02ProviderRunAuthorization`/`IndexingAuthorization`/`QueryAuthorization`), `provbindpn02d` (auth-gated frozen-binding materialization, secret-safe), `routelivepn02d` (`PN02Router`: notebook→workspace→endpoint→storage, wrong-route/unauthorized-membership rejection), `outcomespn02d` (`DriverTechnicalOutcome` + lossless map to `schemaspn02.TechnicalOutcome`), `indexlivepn02d` (submit→poll→terminal, bounded retry, 24/24 gate), `gdlivepn02d` (attestation+query-auth gated GD), `vectorlivepn02d` (candidate restriction BEFORE ranking, one embedding→K3/K5 slices), `removallivepn02d` (endpoint-scoped delete + stale-evidence backstop), `fakeslivepn02d` (index/GD/vector/delete fakes + per-workspace graph topology), `driverpn02d` (B1 orchestrator + dry-run planner + offline simulation), `liveclipn02d` (`validate-live-driver`/`dry-run-b1-plan`/`run-offline-b1-simulation` — NO `execute-b1`). **Authorization chain (fail-closed):** RealPreflight→ProviderRun→binding materialization→Indexing→Query (24/24)→op; `PROVIDER_AUTHORIZATION_PRECEDES_BINDING=YES`; `PN02_PROVIDER_RUN_AUTHORIZED=False` (B0B mints only a controlled SIMULATION auth for fakes). **Full offline B1 simulation reaches the frozen PN02B evaluator** (Stage-1 PASS → R0-R4 → M0-M3) with exact workload (index 24/24, GD 26, vector 26, query-embeddings 26, delete 1, final-answer/client.query/judge 0); membership removal isolates SH_AB (A loses it, B keeps it) and the `post_validate` backstop holds `STALE_GRAPH_EVIDENCE_ACCEPTED_AS_VALID=0` even on a FAILED delete; global-top-K-then-post-filter structurally impossible (backend only sees member candidates); determinism verified; full run completes under explicit socket denial (`B0B_EXTERNAL_PROVIDER_NETWORK_CALLS=0`, `B0B_REAL_LIGHTRAG_BOOT_COUNT=0`, `NORMAL_DB_MUTATIONS=0`). **Tests: 87 B0B offline pass** (authorization/binding/routing/index/query/removal/budget/driver/cli); GraphRAG regression (flag off) **954 pass / 9 skip / 0 fail**; ruff clean on all new files; mypy clean on all 13 B0B modules (only the 5 pre-existing `object`-attr errors in untouched `concurrency_diag08.py` remain, per the 08E.2 baseline). Independent adversarial review (§62 A–T) performed against source + tests. **`B0B_IMPLEMENTATION_READY_FOR_CHECKPOINT=YES` (operator review) · `PN02D_B1_REAUTHORIZATION=NOT_YET` · `PN02_PROVIDER_RUN_AUTHORIZED=NO` · `PN02D_B2_QA_AUTHORIZED=NO`.** Retained frozen: GraphRAG-08 CLOSED/APPROVED, `GRAPH_RAG_09_JUSTIFIED=NO`, `GRAPHRAG_PRODUCTION_INTEGRATION=NOT_APPROVED`, `LIGHTRAG_ASK_INTEGRATION=NOT_APPROVED`, `PRODUCTION_IMPORTS_EVAL=NO`, Boundary B (`SYNTHETIC_ONLY=true`). No migration (count 50); zero provider traffic; sidecar not started; `GRAPHRAG_ENABLED` untouched; no `.env` edit; fixture unchanged; checkpointed at tag `graphrag-pn02db0b-offline-live-driver-approved` (commit `fb21bc1`). | [`GRAPHRAG_PN02DB0B_OFFLINE_LIVE_DRIVER_IMPLEMENTATION.md`](GRAPHRAG_PN02DB0B_OFFLINE_LIVE_DRIVER_IMPLEMENTATION.md) |
| GraphRAG-PN02D-B1-R — Provider run reauthorization preflight | ✅ **REAUTHORIZATION PREFLIGHT APPROVED / OPERATOR AUTHORIZATION JUSTIFIED — checkpointed at tag `graphrag-pn02db1-reauth-preflight-approved`; freezes the ZERO-PROVIDER reauthorization decision ONLY — does NOT authorize B1 execution; no provider request, no provider-bound LightRAG boot, no indexing, no embedding, no `/query/data`, no `client.query`, no live vector query, no final answers, no B1 execution, no B2, no production Ask/GraphRAG, no RRF, no GraphRAG-09; no `PN02ProviderRunAuthorization` minted.** Zero-provider gate evaluating whether the historically BLOCKED PN02D-B1 run is now safe + technically valid to re-authorize after the approved B0B offline live driver. Baseline verified from git (branch `feature/graphrag-lifecycle`, HEAD `2e76cf5`, tree CLEAN; B0B tag `graphrag-pn02db0b-offline-live-driver-approved` peel `fb21bc1`; B0A tag peel `a2e8303`; PN02D-A tag peel `8e09617`). **`IMPLEMENTATION_TREE_UNCHANGED_SINCE_B0B=YES`** — HEAD `2e76cf5` vs tagged B0B `fb21bc1` = CURRENT_PHASE.md docs-only (2 lines). Fixture `graphrag_pn02_eval_v1` revalidated `FIXTURE_VALIDATION=PASS`/`FIXTURE_HASH_VALIDATION=PASS` (`9ce7df74…6899a6`; 3 nb / 21 src / 24 memberships / 24 queries). **Source-verified (13 B0B modules):** fail-closed capability chain RealPreflight→PN02ProviderRunAuthorization→binding→Indexing→Query(24/24) — `isinstance`-gated (no boolean bypass), private-sentinel unforgeable, fixture-hash pinned at mint; `PROVIDER_AUTHORIZATION_PRECEDES_BINDING=YES`; allowlist = {GRAPH_INDEX, INDEX_REQUIRED_EMBEDDING, GD_QUERY_DATA, VECTOR_QUERY_EMBEDDING, VECTOR_NOTEBOOK_QUERY, GRAPH_DELETE}, forbidden {CLIENT_QUERY, FINAL_ANSWER, QA_*, JUDGE, ASK} structurally rejected + hard-0 caps; per-notebook routing (record-id derived, wrong-route + non-member rejected); doc identity `(workspace_id, canonical_source_id)`, endpoint≠identity, cross-workspace delete refused; exact notebook-local vector (candidate restriction BEFORE ranking → `GLOBAL_TOPK_THEN_POSTFILTER_PRESENT=NO`, K3/K5 one ranking); stateful pre-op budget (projected-count check before dispatch); technical-vs-scientific error taxonomy; owned-only `finally` cleanup. **Runtime evidence (offline, external-network denied):** dry-run plan exact (24 index / SH_AB→{A,B} SH_AC→{A,C} SH_BC→{B,C} / GD 24+2 / vector 24+2 / delete 1 at NB_A / caps 24-48-1-26-26-26-0-0-0); full offline simulation COMPLETE, 24/24 indexed, budgets exactly at plan, `provider_traffic=0`, isolation evidenced, Stage-1/R0-R4/M0-M3 reached; 23/24→`FAILED_BEFORE_QUERY` (0 queries); stale-evidence (FAILED delete) `STALE_GRAPH_EVIDENCE_ACCEPTED_AS_VALID=0`; `PRODUCTION_IMPORTS_EVAL=NO` (guard test + independent grep). Provider config identity (name-only, no call): OpenRouter · `openai/gpt-4o-mini` · `openai/text-embedding-3-small` · dim 1536 · secret-free fingerprint `pbf_1811d0bfd5cfad2743ffaa69`; `OPENROUTER_KEY_PRESENT=YES` (in `.env`, value never read), `SIDE_CAR_AUTH_CONFIG_PRESENT=NOT_REQUIRED`; `LIGHTRAG_IMAGE_PIN=v1.5.6` `LIGHTRAG_IMAGE_AVAILABLE=YES` (local inspect, no pull). Concurrency index/GD/vector=1 (sequential, `UNBOUNDED_PROVIDER_CONCURRENCY=NO`); retry ≤2/op (48 total), transient-only, `SCIENTIFIC_RESULT_RETRY_ALLOWED=NO`. **Tests:** 87 targeted B0B · 140 full PN02 reauth set · 954 pass / 9 skip / 0 fail GraphRAG+PN02 regression (no new regressions). **Independent adversarial review (§42 A–T): 0 HIGH / 0 MEDIUM / 2 LOW** (LOW-1 executors trust auth type without `isinstance` in `__init__` — fail-closed via mint/allowlist; LOW-2 `run_id` not cross-checked against the capability — fixture-hash pinned + fixture frozen + offline fakes-only; both non-blocking hardening notes for the future B1 wiring). **`REAUTH_DECISION=C — B1_REAUTHORIZATION_OPERATOR_APPROVAL_JUSTIFIED`; `PN02D_B1_REAUTHORIZATION_GATE=PASS`; `PN02D_B1_OPERATOR_AUTHORIZATION_JUSTIFIED=YES`. C does NOT mean `PN02D_B1_PROVIDER_RUN_AUTHORIZED=YES`** — a separate explicit operator authorization (bound to one run_id + this fixture hash + B0B checkpoint identity + provider-config fingerprint + synthetic-only + workload caps) is required before any traffic; this preflight minted NO `PN02ProviderRunAuthorization`. Retained frozen: PN02D-B0B = APPROVED, PN02D-B1 = historical BLOCKED preserved, **`PN02D_B1_PROVIDER_RUN_AUTHORIZED=NO`**, `PN02_PROVIDER_RUN=NOT_AUTHORIZED`, PN02D-B2 = NOT_AUTHORIZED, GraphRAG-08 CLOSED/APPROVED, `GRAPH_RAG_09_JUSTIFIED=NO`, `GRAPHRAG_PRODUCTION_INTEGRATION=NOT_APPROVED`, `LIGHTRAG_ASK_INTEGRATION=NOT_APPROVED`, Boundary B (`SYNTHETIC_ONLY=true`). No code change; no migration (count 50); zero provider traffic; sidecar not started; `GRAPHRAG_ENABLED` untouched; no `.env` edit; fixture unchanged; NOT checkpointed. | [`GRAPHRAG_PN02DB1_PROVIDER_RUN_REAUTHORIZATION_PREFLIGHT.md`](GRAPHRAG_PN02DB1_PROVIDER_RUN_REAUTHORIZATION_PREFLIGHT.md) |
| GraphRAG-PN02D-B1 — Operator-authorized provider run (attempt #2) | ⛔ **BLOCKED — pre-provider; ZERO provider traffic occurred and none was possible; ONE-RUN OPERATOR AUTHORIZATION NOT CONSUMED; NOT checkpointed (operator review required).** `AUTHORIZATION_RUN_ID=pn02db1-daf6b760-7d68-4674-9222-ac9f962ef6c4`. Under an explicit one-run operator authorization, all zero-provider prechecks were run and PASSED: baseline verified from git (branch `feature/graphrag-lifecycle`, HEAD `42122b0`, tree CLEAN; B0B tag `graphrag-pn02db0b-offline-live-driver-approved` peel `fb21bc1`; B1-R tag `graphrag-pn02db1-reauth-preflight-approved` peel `42122b0`), fixture `graphrag_pn02_eval_v1` revalidated `FIXTURE_VALIDATION=PASS`/`FIXTURE_HASH_VALIDATION=PASS` (`9ce7df74…6899a6`), provider-config fingerprint MATCH (`pbf_1811d0bfd5cfad2743ffaa69`; OpenRouter · `openai/gpt-4o-mini` · `openai/text-embedding-3-small` · dim 1536), `OPENROUTER_API_KEY` present in `.env` (bool only, never read), Docker up, LightRAG `v1.5.6` config-pinned. Execution then **STOPPED before any provider traffic** at the §11 real-LightRAG-preflight / §14 index-executor precondition: **the committed B0B harness has NO real provider-backed B1 execution path** (source-verified per AGRIBANK §1). `liveclipn02d` deliberately has no `execute-b1` verb ("no provider, no LightRAG, no network"); `B1OfflineDriver` takes every provider/LightRAG/DB seam via injected Protocols and the ONLY constructor of `B1DriverDeps` is `run_offline_b1_simulation` using **fakes** (`build_clean_fake_topology`); no real GD/vector/delete backend adapters exist and the `live_indexer08.RealCellIndexClient` adapter is a design-note only (`indexlivepn02d.py:68`), not implemented; `PN02_PROVIDER_RUN_AUTHORIZED=False` and the only auth mint feeds **simulated** gate booleans (`build_simulation_provider_run_authorization`); routing yields `eval-null://` placeholder endpoints; `lightrag`/`lightrag-hku` not installed in-process (the PN02D-A Docker path boots provider-FREE, does NOT index, and does NOT drive the B1 orchestrator). Building the missing ~5 provider-facing, security-sensitive subsystems (real index/GD/vector/delete adapters + real provider-bound 3-runtime LightRAG boot-and-drive orchestration + real gate-0/gate-1 computation + real `PN02ProviderRunAuthorization` mint from a real preflight + `execute-b1` path) and firing them once on the sole one-run authorization with **no offline validation/tests/independent review** is prohibited (AGRIBANK §2/§6/§11/§12) — so it was NOT built this turn. Consequently `PN02ProviderRunAuthorization` was **never minted** and `AUTHORIZATION_CONSUMED=NO`; `INDEXED_WORKSPACE_MEMBERSHIPS=0/24 → FAILED_BEFORE_QUERY`; Stage-1, isolation, retrieval R0–R4, multi-hop M0–M3, membership-removal all `NOT_EVALUATED`; every workload category `0` (`STALE_GRAPH_EVIDENCE_ACCEPTED_AS_VALID=0`, `NORMAL_DB_MUTATIONS=0`, `OWNED_LIGHTRAG_PROCESSES_REMAINING=0`, `OWNED_RUNTIME_STORAGE_RESIDUE=0`, `OWNED_NETWORK_RESIDUE=0`). **`B1_DECISION=A — B1_TECHNICAL_EXECUTION_FAILED (BLOCKED_PRE_PROVIDER_NO_REAL_EXECUTION_PATH)`**; `PN02D_B2_QA_AUTHORIZATION_GATE_JUSTIFIED=NO`; `PN02D_B2_QA_AUTHORIZED=NO`; `PER_NOTEBOOK_GRAPH_QA_VALUE_EVIDENCED=NOT_YET`. Zero-traffic evidence via `liveclipn02d validate-live-driver` (`provider_traffic=0`); no implementation code changed (this row + result doc only). **Recommended next step:** a separate offline live-wiring phase (zero traffic) to build + independently review the real B1 wiring, then re-authorize B1. Retained frozen: GraphRAG-08 CLOSED/APPROVED, `PN02_PROVIDER_RUN=NOT_AUTHORIZED`, `GRAPH_RAG_09_JUSTIFIED=NO`, `GRAPHRAG_PRODUCTION_INTEGRATION=NOT_APPROVED`, `LIGHTRAG_ASK_INTEGRATION=NOT_APPROVED`, Boundary B (`SYNTHETIC_ONLY=true`). No provider traffic; sidecar not started; `GRAPHRAG_ENABLED` untouched; no `.env` edit; no migration (count 50); NOT checkpointed. | [`GRAPHRAG_PN02DB1_LIVE_INDEXED_ISOLATION_GD_RESULT.md`](GRAPHRAG_PN02DB1_LIVE_INDEXED_ISOLATION_GD_RESULT.md) |
| GraphRAG-PN02B — Synthetic per-notebook evaluation harness (OFFLINE implementation) | 🟨 **OFFLINE HARNESS IMPLEMENTED — awaiting operator review; NOT checkpointed; no provider traffic, no live LightRAG, no live `/query/data`, no final-answer LLM, no real internal data, no Ask/GraphRAG production integration, no RRF, no GraphRAG-09.** Implements the frozen PN02A design (baseline HEAD `5415667`) without reinterpreting topology/taxonomy/GT/metrics/gates. **Frozen fixture `graphrag_pn02_eval_v1`** (new; `graphrag_08_eval_v1` + hash untouched): 3 notebooks, 21 canonical Sources (18 unique + 3 shared, ONE record per shared Source), 24 membership edges, 8 Sources/notebook, 24 queries (8 classes × 3); Orion collision AX-17/BX-94/CX-51; symmetric-negative cycle (A→B→C→A); SH_AB is the NB_A/NB_B removal re-probe pair. **`PN02_FIXTURE_HASH = 9ce7df742810424d9ef7b7b34962187d9061245f81471f5fb1d7f2608f6899a6`** (SHA-256 over canonical sorted-key JSON of {notebooks, sources, memberships, queries+GT}, PN02A §16; mutation-guard test). **14 eval-only modules** under `integrations/graphrag/eval/` (`*pn02*`, never imported by production — verified): dataset/loader+validator+hash, normalize (canonical fixture ids, provenance foreign/malformed), schemas (V/GD/QA/removal + `TechnicalOutcome` error taxonomy vs `ScienceVerdict`), metrics (exact §9/§9c/§10d formulas + deterministic grader), combine (frozen QA-V+GD, no RRF/rank/score), stage1 (HARD gate + unforgeable `Stage1Authorization` capability → **`STAGE_2_AFTER_STAGE_1_FAILURE_POSSIBLE = NO`** structural), decisions (R0–R4/Q0–Q3/M0–M3 first-match tables + exact 8/8 trap, no fuzzy thresholds), membership (removal evaluator + defense-in-depth backstop → `STALE_GRAPH_EVIDENCE_ACCEPTED_AS_VALID=0`), workload (ledger 24/48/1/26/26/26/72/judge0 + `check_cap`), manifest (routing `nb_+sha256(record_id)[:16]`, attestation + no-unattested-query, version pin `v1.5.6`, Boundary-B guard), inert live seam (Protocols + non-invokable stubs, **no HTTP client**), report (content-safe), offline orchestrator (fail-closed Stage-2), OFFLINE CLI (`validate`/`hash`/`evaluate-results` — no `run-benchmark` verb). **Tests: 93 PN02B offline pass** (fixture/stage1/retrieval/qa/multihop/membership/workload/manifest/combine/offline — incl. 5 negative isolation FAILs + positive PASS, forbidden-fact-gain-cannot-be-YES, stale-evidence defense-in-depth, socket-blocked network denial, no-production-import); GraphRAG regression (flag off) **803 pass / 9 skip / 0 fail**; ruff + mypy clean on all 14 modules; git audit clean (all additive, no secrets, no production/tracked file modified except this row + PN02B doc). **`PN02B_STATUS = OFFLINE_COMPLETE`, `PN02_LIVE_AUTHORIZED = NO`; next = GraphRAG-PN02C (live preflight/provisioning gate, NOT_STARTED, NOT authorized).** Retained frozen: `GRAPHRAG_PRODUCTION_INTEGRATION=NOT_APPROVED`, `LIGHTRAG_ASK_INTEGRATION=NOT_APPROVED`, `RRF_CANDIDATE_INTERFACE_READY=NO`, `STRUCTURED_EVIDENCE_IMPLEMENTATION_READY=NO`, **`GRAPH_RAG_09_JUSTIFIED=NO`**, `PRODUCTION_IMPORTS_EVAL=NO`. Boundary B unchanged (synthetic-only). No provider traffic; sidecar not started; `GRAPHRAG_ENABLED` untouched; no `.env`; no migration (count 50); NOT checkpointed. | [`GRAPHRAG_PN02B_SYNTHETIC_EVAL_HARNESS_IMPLEMENTATION.md`](GRAPHRAG_PN02B_SYNTHETIC_EVAL_HARNESS_IMPLEMENTATION.md) · `tests/fixtures/graphrag_pn02_eval_v1/` |

**PN02D governance states:** PN02D-A = APPROVED · **PN02D-B1 = BLOCKED — LIVE DRIVER ABSENT** (technical precondition blocker before any provider traffic; `B1_SCIENTIFIC_RESULT=NOT_EVALUATED`; NOT a LightRAG/retrieval/isolation/provider failure) · **PN02D-B0A = LIVE DRIVER DESIGN APPROVED** (design/forensic COMPLETE + refinement B0A-R1; `B0A_DECISION=C` LIVE_DRIVER_DESIGN_FROZEN_AND_OFFLINE_IMPLEMENTATION_JUSTIFIED; `PN02D_B0B_IMPLEMENTATION_JUSTIFIED=YES`; checkpointed at tag `graphrag-pn02db0a-live-driver-design-approved`, commit `a2e8303`) · **PN02D-B0B = OFFLINE IMPLEMENTATION APPROVED** (13 eval-only driver modules + 87 offline tests; checkpointed at tag `graphrag-pn02db0b-offline-live-driver-approved`, commit `fb21bc1`; ZERO provider traffic; separate operator authorization required before B1) · **PN02D-B1-REAUTH (B1-R) = REAUTHORIZATION PREFLIGHT APPROVED / OPERATOR AUTHORIZATION JUSTIFIED** (`REAUTH_DECISION=C`; `PN02D_B1_REAUTHORIZATION_GATE=PASS`; independent review 0 HIGH / 0 MEDIUM / 2 LOW-nonblocking; zero provider traffic; checkpointed at tag `graphrag-pn02db1-reauth-preflight-approved`; `PN02D_B1_PROVIDER_RUN_AUTHORIZED=NO` — a separate explicit operator authorization is still required before any B1 traffic) · **PN02D-B1 (operator-authorized attempt #2, run `pn02db1-daf6b760…ac9f962ef6c4`) = BLOCKED — PRE-PROVIDER, NO REAL EXECUTION PATH** (all zero-provider prechecks PASSED — baseline/fixture-hash/provider-fingerprint `pbf_1811d0bfd5cfad2743ffaa69` — but the committed B0B driver is OFFLINE-ONLY: injected Protocols implemented only by fakes, no `execute-b1` verb, no real backend adapters, no real-LightRAG boot-and-drive orchestration; building+firing that under a one-run authorization with no offline review is prohibited per AGRIBANK §2/§6/§11/§12; `B1_DECISION=A`, `AUTHORIZATION_CONSUMED=NO`, zero provider traffic, 0/0/0 residue; authorization `pn02db1-daf6b760…ac9f962ef6c4` now **RETIRED** — `AUTHORIZATION_REUSABLE=NO`, `AUTHORIZATION_STATUS=RETIRED_AFTER_BLOCKED_EXECUTION_ATTEMPT`, `AUTHORIZATION_RETIRE_REASON=IMPLEMENTATION_PATH_INCOMPLETE`; a future B1 needs a NEW impl checkpoint → NEW reauth preflight → NEW operator authorization → NEW run_id) · **PN02D-B0C-A (Real Provider Adapter & Execution Wiring Design) = NOT_STARTED / DESIGN GATE JUSTIFIED** (`PN02D_REAL_WIRING_DESIGN_GATE_JUSTIFIED=YES`; DESIGN/FORENSIC only when started) · **PN02D-B0C-B = NOT_STARTED** · **PN02D-B1-R2 = NOT_STARTED** · **PN02D-B2 = NOT_AUTHORIZED** · `PN02_PROVIDER_RUN = NOT_AUTHORIZED`. **Future code-review governance (B0C-B and later code phases):** CLAUDE_CODE_IMPLEMENTATION → CODEX_INDEPENDENT_CODE_REVIEW → CLAUDE_CODE_REMEDIATION → CODEX_RE_REVIEW → (only if Codex PASS) → OPERATOR_CHECKPOINT_APPROVAL; Claude internal review supplements but does not replace Codex review (B0C-A is docs/design only → no Codex code review required).

**GraphRAG-09:** NOT_STARTED / NOT_JUSTIFIED.

**Branch:** `feature/graphrag-lifecycle` (from `bc5b413`) · **LightRAG pinned:** `v1.5.6`

### GraphRAG-03 slice status
| Slice | Status |
|---|---|
| 03-A INDEX/REINDEX | ✅ **COMPLETE** — approved 2026-08-28 (no migration; count 46) |
| 03-B Durable deletion state + DB event (migration) | ✅ **COMPLETE / APPROVED** — signed off 2026-08-28. Migration **24** (`graphrag_deletion` SCHEMAFULL table + separate `graphrag_source_delete` event, Option A / flag-independent) with a per-arm **`arm_id`** (`rand::uuid()`) fence token closing the ABA re-arm race (verified on SurrealDB v2.6.5); read-only tombstone helper; 28 property tests. Migration count 46 → **48**. No HTTP/egress. Karpathy CLEAN · Codex APPROVE. See [`GRAPHRAG_03B_DURABLE_DELETE.md`](GRAPHRAG_03B_DURABLE_DELETE.md). |
| 03-C Tombstone draining / retry / idempotent delete | ✅ **COMPLETE / APPROVED** — signed off 2026-08-30. Migration **25** (`next_attempt_at` fair-drain field + event OVERWRITE; migration 24 frozen). Confirmed-absence probe via `POST /documents/paginated` single-response snapshot (`deletion_started` ≠ absence); `arm_id` CAS resolve/defer (stale CAS = 0 rows); live-source convergence (flag-OFF→absent, no Boundary-B egress; live-empty→absent; live-current→CURRENT canonical); small cancellable periodic wake-up in the FastAPI lifespan (startup kick + best-effort `Source.delete` wake-up; no new scheduler); durable tombstone is the work source-of-truth, re-drive independent of the crash-prone command queue; bounded due-set traversal (no OFFSET). 03C 42 tests (2 live-LightRAG against pinned v1.5.6) + 288 GraphRAG regression + full backend 936 pass / 2 skipped / 5 pre-existing baseline; ruff+mypy clean. Migration count 48 → **50**. Karpathy CLEAN · Codex A APPROVE · Codex B APPROVE. **Accepted limitation:** the >200-doc single-response absence ceiling stays `UNKNOWN`/pending (remote delete still re-driven; tombstone never falsely resolved; scalable proof deferred to 03D). See [`GRAPHRAG_03C_TOMBSTONE_DRAIN.md`](GRAPHRAG_03C_TOMBSTONE_DRAIN.md). |
| 03-D RECONCILE | ✅ **COMPLETE / APPROVED** — signed off 2026-08-30. Defense-in-depth reconcile: bounded streaming remote sweep + STRONG ownership proof (`file_path` lossless `source` id AND `compute_doc_id`==doc.id); owned orphan / live-empty / flag-OFF → **arm durable 03B/03C tombstone** (new `deletion.arm_orphan_deletion`, DB-generated `arm_id`, no re-arm churn) drained by 03C; authoritative-missing (single-response snapshot, flag ON) → `source_id`-only 03A enqueue; FOREIGN/UNKNOWN/PRESENT_UNVERIFIED report-only. AUDIT default, REPAIR opt-in; `graphrag_reconcile` surreal-command (**no scheduler, no new API**). **NO migration 26** (24/25 frozen; count stays 50). **Forensic GATE (task §9/§35):** pinned v1.5.6 cannot prove scalable authoritative absence (offset paging ≤200, no by-id lookup, no corpus cursor, no `content_hash`) → 03D does **not** resolve tombstones and does **not** claim the >200 ceiling solved (documented blocker; needs upstream by-id/keyset). 54 new tests (mock + live SurrealDB v2.6.5 + live LightRAG v1.5.6); GraphRAG regression 338 pass / 1 skip; full backend 983 pass / 6 skip / 5 pre-existing baseline failures unchanged; ruff+mypy clean. **Karpathy CLEAN · Codex A APPROVE · Codex B resolved · Codex C (via `codex review`) P2/P3 resolved, final re-review no new actionable HIGH/MEDIUM.** No unresolved actionable HIGH. See [`GRAPHRAG_03D_RECONCILE.md`](GRAPHRAG_03D_RECONCILE.md). |
| 03-E REBUILD | ✅ **COMPLETE / APPROVED** — signed off 2026-08-30. Operator-triggered canonical rebuild **dispatcher** that re-drives the existing 03A `graphrag_index_source` (source_id ONLY) over CURRENT non-empty sources to force convergence of PRESENT_UNVERIFIED docs. **PLAN** (read-only, default: enumerate/classify/count; no health/HTTP/enqueue/arm/mutation) vs **EXECUTE** (explicit; gate flag+config+content-free `GET /health` BEFORE any dispatch → source_id-only enqueue). **Decision A:** empty sources reported, never armed/dispatched (cleanup stays 03D→03B/03C); 03E is a dispatcher, not a deletion orchestrator. Keyset traversal + **RecordID** cursor (no OFFSET; numeric≠string-numeric; invalid cursor fails closed); `max_sources_per_run` fairness cap with a look-ahead so the exact boundary N==cap vs N==cap+1 never false-completes. Honest completion vocabulary — `REBUILD_DISPATCH_COMPLETE` = this sweep/continuation fully dispatched (no continuation, no enqueue failure); never INDEX_COMMAND_COMPLETION, never REMOTE_CONTENT_CONVERGENCE_VERIFIED (no `content_hash` on v1.5.6). No global purge, no foreign-doc deletion, no automatic/scheduled trigger, no `full_text` payload. **NO migration 26** (24/25 frozen; count stays **50**). **Dedicated default-OFF EXECUTE lock** (`OPEN_NOTEBOOK_GRAPHRAG_REBUILD_EXECUTE_ENABLED`, checked first) so enabling GraphRAG for ingestion never unlocks a corpus-wide Boundary-B rebuild (Codex-B). **Cursor skip-safety:** a per-row failure (invalid id / state-read error / enqueue failure) fail-stops the sweep and never advances the resumable cursor past an un-handled source, so a resume re-attempts it and the run is DISPATCH_PARTIAL, never a false COMPLETE (Codex-A/C). New `rebuild.py` + `GraphRAGRebuildConfig` + `graphrag_rebuild` command (`max_attempts=1`); 43 property tests (mock + 2 live SurrealDB v2.6.5 + **3 live LightRAG v1.5.6 through a REAL surreal-commands worker, PASSED** — real EXECUTE → source_id-only dispatch → worker reloads CURRENT source → document present in the real sidecar; execute-lock-off and preflight-failure zero-dispatch invariants). GraphRAG regression 373 pass; full backend 1023 pass / 9 skip / 5 pre-existing platform failures; ruff clean; production mypy clean. **Karpathy CLEAN · Codex A/B/C run — all HIGH findings resolved with regression tests, re-verified green; no unresolved actionable HIGH/MEDIUM.** **Live LightRAG synthetic EXECUTE acceptance gate PASSED (synthetic-only; env restored, no worker leak, no secrets in tree).** See [`GRAPHRAG_03E_REBUILD.md`](GRAPHRAG_03E_REBUILD.md). **Karpathy CLEAN · Codex A/B/C resolved · no unresolved actionable HIGH/MEDIUM.** |

> **Boundary B (sidecar → LLM/embedding provider) is NOT APPROVED for internal data.** Real internal data is **prohibited** in every phase until a separate egress decision exists. Synthetic, public, or anonymized content only.

---

## GraphRAG-02 — completed scope

An isolated, flag-gated LightRAG integration boundary with experimental diagnostic endpoints. Default **OFF**; not wired into source ingestion, Ask, or Chat.

Delivered: `open_notebook/integrations/graphrag/` (config · models · client · service) · `api/routers/graphrag.py` (+2 lines in `api/main.py`) · three test modules · dev-only sidecar compose profile · frozen contract documentation.

### Acceptance criteria (§21.12) — all met

- [x] LightRAG boundary fully isolated; never imported or vendored
- [x] Flag defaults OFF; baseline unchanged when off
- [x] Open Notebook boots and operates without LightRAG present
- [x] Synthetic document indexable via the PoC path
- [x] Experimental graph query works
- [x] Failures normalized; no `httpx` exception escapes
- [x] Metadata allowlist verified at field **and value** level
- [x] No real internal data used
- [x] No existing RAG path modified
- [x] No source ingestion path modified
- [x] No DB migration (count unchanged at 46)
- [x] Tests pass
- [x] Karpathy diff clean (2 passes, 5 findings resolved)
- [x] Codex review: no unresolved HIGH (2 passes, 4 findings resolved)
- [x] User sign-off — 2026-08-27

### Verification record

```text
Date:    2026-08-27
Branch:  feature/graphrag-lightrag
LightRAG pinned: v1.5.6 (contract read from router source, not docs)

GraphRAG tests:  189 passed
Backend pytest:  839 passed, 5 failed
                 (all 5 proven pre-existing: stashing every GraphRAG file
                  reproduced them at 5 failed / 50 passed)
Backend ruff:    All checks passed
Backend mypy:    Success — no issues
Frontend:        untouched (verified by test)
Migrations:      unchanged (46 files)

Karpathy diff:   clean — 5 findings across 2 passes, all resolved
Codex review:    no unresolved HIGH — 4 findings across 2 passes, all resolved
RecordID:        canonical round trip verified; numeric vs numeric-string
                 identity preserved; LightRAG file_source compatibility verified
Commit:          approved for commit; nothing committed automatically
```

### Known baseline failures (not GraphRAG-related, not fixed here)

Five backend tests fail independently of this work — four Windows-environment artifacts (symlink privilege, path separators, `file://` drive letters) and one genuine logic bug in proxy env-var case merging. Full evidence and per-test detail in [`../architecture/GRAPHRAG_POC.md`](../architecture/GRAPHRAG_POC.md) § Known Baseline Test Failures.

---

## Blockers for later phases

| Blocker | Blocks |
|---|---|
| **Boundary B not approved** | Any real internal data, in every phase |
| Deletion-durability mechanism undecided | GraphRAG-03; an outbox would need its own migration approval |
| No synthetic evaluation corpus | GraphRAG-04 — without it, later phases optimize an unmeasured system |
| Is notebook-scoped retrieval needed today? | Whether `RetrievalScope` ships used or reserved |

## Next phase

**GraphRAG-03 is COMPLETE** — all slices (03-A … 03-E) approved. **GraphRAG-03E** — REBUILD (operator-triggered bounded canonical rebuild dispatcher; forced convergence of PRESENT_UNVERIFIED docs) — ✅ **COMPLETE / APPROVED 2026-08-30**: PLAN default (strictly read-only) / explicit EXECUTE behind a dedicated default-OFF lock; content-free preflight before dispatch; bounded keyset traversal + lossless RecordID continuation cursor + hard source cap; `last_good_id` fail-stop so an incomplete/partial sweep is never reported complete; source_id-only reuse of the 03A lifecycle (03A reloads CURRENT Source); empty Sources report-only; no tombstone mutation, no global purge, no foreign/unknown mutation, no `full_text` payload; `REBUILD_DISPATCH_COMPLETE` is dispatch-only (never a remote-content-verification claim). **NO migration 26** (24/25 frozen; count 50). Live LightRAG v1.5.6 synthetic EXECUTE gate PASSED (real worker + real sidecar; execute-lock-off and preflight zero-partial-dispatch verified; CURRENT-source A→B reload verified). Karpathy CLEAN; Codex A/B/C resolved; no unresolved actionable HIGH/MEDIUM.

**Accepted limitations (carried forward):** LightRAG v1.5.6 exposes no `content_hash`, so a successful 03A execution means the CURRENT canonical Source was submitted through the approved lifecycle — NOT that remote content equality was independently verified; REBUILD is not a globally atomic snapshot; `REBUILD_DISPATCH_COMPLETE` is dispatch completion for the bounded sweep/continuation only. The >200-doc authoritative-absence ceiling remains an upstream-capability blocker (03C/03D).

**No later GraphRAG phase (04+) has been started.** Boundary B remains NOT APPROVED for real internal data; any real-data rebuild needs a separate egress decision.
